openapi: 3.1.0
info:
  # Do not change the title, if the title changes, the import paths will be broken
  title: Api
  version: 0.2.0
  description: RankMap API specification
servers:
  - url: /api
    description: Base API path
tags:
  - name: health
  - name: auth
  - name: clients
  - name: projects
  - name: keywords
  - name: clusters
  - name: briefs
  - name: reports
  - name: ai-tasks
  - name: tenant
  - name: billing
  - name: team
  - name: integrations
  - name: api-keys
  - name: webhooks
  - name: notifications
  - name: geo-aeo

paths:
  /healthz:
    get:
      operationId: healthCheck
      tags: [health]
      summary: Health check
      responses:
        "200":
          description: Healthy
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/HealthStatus"

  /auth/register:
    post:
      operationId: registerUser
      tags: [auth]
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/RegisterBody"
      responses:
        "201":
          description: Account created
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AuthResponse"
        "400":
          description: Validation error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "409":
          description: Email already in use
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"

  /auth/login:
    post:
      operationId: loginUser
      tags: [auth]
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/LoginBody"
      responses:
        "200":
          description: Logged in
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AuthResponse"
        "401":
          description: Invalid credentials
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"

  /auth/logout:
    post:
      operationId: logoutUser
      tags: [auth]
      responses:
        "204":
          description: Logged out

  /auth/me:
    get:
      operationId: getCurrentUser
      tags: [auth]
      responses:
        "200":
          description: Current user
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AuthResponse"
        "401":
          description: Not authenticated
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"

  /tenants/me:
    get:
      operationId: getMyTenant
      tags: [tenant]
      responses:
        "200":
          description: Tenant
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Tenant"
        "401":
          description: Not authenticated
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
    patch:
      operationId: updateMyTenant
      tags: [tenant]
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/UpdateTenantBody"
      responses:
        "200":
          description: Updated tenant
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Tenant"

  /dashboard:
    get:
      operationId: getDashboard
      tags: [tenant]
      responses:
        "200":
          description: Dashboard summary
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/DashboardSummary"
        "401":
          description: Not authenticated
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"

  /clients:
    get:
      operationId: listClients
      tags: [clients]
      responses:
        "200":
          description: List of clients
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/Client"
    post:
      operationId: createClient
      tags: [clients]
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CreateClientBody"
      responses:
        "201":
          description: Created client
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Client"
        "400":
          description: Validation error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"

  /clients/{id}:
    get:
      operationId: getClient
      tags: [clients]
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
      responses:
        "200":
          description: Client
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Client"
        "404":
          description: Not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
    patch:
      operationId: updateClient
      tags: [clients]
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/UpdateClientBody"
      responses:
        "200":
          description: Updated client
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Client"
        "404":
          description: Not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
    delete:
      operationId: deleteClient
      tags: [clients]
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
      responses:
        "204":
          description: Deleted
        "404":
          description: Not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"

  /projects:
    get:
      operationId: listProjects
      tags: [projects]
      parameters:
        - name: clientId
          in: query
          required: false
          schema:
            type: integer
      responses:
        "200":
          description: List of projects
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/Project"
    post:
      operationId: createProject
      tags: [projects]
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CreateProjectBody"
      responses:
        "201":
          description: Created project
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Project"
        "400":
          description: Validation error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"

  /projects/{id}:
    get:
      operationId: getProject
      tags: [projects]
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
      responses:
        "200":
          description: Project
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Project"
        "404":
          description: Not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
    patch:
      operationId: updateProject
      tags: [projects]
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/UpdateProjectBody"
      responses:
        "200":
          description: Updated project
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Project"
        "404":
          description: Not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
    delete:
      operationId: deleteProject
      tags: [projects]
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
      responses:
        "204":
          description: Deleted
        "404":
          description: Not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"

  /projects/{projectId}/keywords:
    get:
      operationId: listKeywords
      tags: [keywords]
      parameters:
        - name: projectId
          in: path
          required: true
          schema:
            type: integer
        - name: clusterId
          in: query
          required: false
          schema:
            type: integer
        - name: intent
          in: query
          required: false
          schema:
            type: string
        - name: isActive
          in: query
          required: false
          schema:
            type: boolean
      responses:
        "200":
          description: List of keywords
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/Keyword"
    post:
      operationId: createKeyword
      tags: [keywords]
      parameters:
        - name: projectId
          in: path
          required: true
          schema:
            type: integer
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CreateKeywordBody"
      responses:
        "201":
          description: Created keyword
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Keyword"

  /projects/{projectId}/keywords/import:
    post:
      operationId: importKeywords
      tags: [keywords]
      parameters:
        - name: projectId
          in: path
          required: true
          schema:
            type: integer
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/ImportKeywordsBody"
      responses:
        "200":
          description: Import result
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ImportKeywordsResponse"

  /projects/{projectId}/keywords/{id}:
    get:
      operationId: getKeyword
      tags: [keywords]
      parameters:
        - name: projectId
          in: path
          required: true
          schema:
            type: integer
        - name: id
          in: path
          required: true
          schema:
            type: integer
      responses:
        "200":
          description: Keyword
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Keyword"
        "404":
          description: Not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
    patch:
      operationId: updateKeyword
      tags: [keywords]
      parameters:
        - name: projectId
          in: path
          required: true
          schema:
            type: integer
        - name: id
          in: path
          required: true
          schema:
            type: integer
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/UpdateKeywordBody"
      responses:
        "200":
          description: Updated keyword
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Keyword"
    delete:
      operationId: deleteKeyword
      tags: [keywords]
      parameters:
        - name: projectId
          in: path
          required: true
          schema:
            type: integer
        - name: id
          in: path
          required: true
          schema:
            type: integer
      responses:
        "204":
          description: Deleted

  /projects/{projectId}/score-settings:
    get:
      operationId: getScoreSettings
      tags: [keywords]
      parameters:
        - name: projectId
          in: path
          required: true
          schema:
            type: integer
      responses:
        "200":
          description: Score settings
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ScoreSettings"
    patch:
      operationId: updateScoreSettings
      tags: [keywords]
      parameters:
        - name: projectId
          in: path
          required: true
          schema:
            type: integer
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/UpdateScoreSettingsBody"
      responses:
        "200":
          description: Updated score settings
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ScoreSettings"

  /projects/{projectId}/clusters:
    get:
      operationId: listClusters
      tags: [clusters]
      parameters:
        - name: projectId
          in: path
          required: true
          schema:
            type: integer
        - name: status
          in: query
          required: false
          schema:
            type: string
      responses:
        "200":
          description: List of clusters
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/KeywordCluster"
    post:
      operationId: createCluster
      tags: [clusters]
      parameters:
        - name: projectId
          in: path
          required: true
          schema:
            type: integer
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CreateClusterBody"
      responses:
        "201":
          description: Created cluster
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/KeywordCluster"

  /projects/{projectId}/cluster-keywords:
    post:
      operationId: clusterKeywords
      tags: [clusters]
      parameters:
        - name: projectId
          in: path
          required: true
          schema:
            type: integer
      responses:
        "202":
          description: Clustering task queued
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AiTask"

  /projects/{projectId}/clusters/{id}:
    get:
      operationId: getCluster
      tags: [clusters]
      parameters:
        - name: projectId
          in: path
          required: true
          schema:
            type: integer
        - name: id
          in: path
          required: true
          schema:
            type: integer
      responses:
        "200":
          description: Cluster
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/KeywordCluster"
        "404":
          description: Not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
    patch:
      operationId: updateCluster
      tags: [clusters]
      parameters:
        - name: projectId
          in: path
          required: true
          schema:
            type: integer
        - name: id
          in: path
          required: true
          schema:
            type: integer
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/UpdateClusterBody"
      responses:
        "200":
          description: Updated cluster
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/KeywordCluster"
    delete:
      operationId: deleteCluster
      tags: [clusters]
      parameters:
        - name: projectId
          in: path
          required: true
          schema:
            type: integer
        - name: id
          in: path
          required: true
          schema:
            type: integer
      responses:
        "204":
          description: Deleted

  /projects/{projectId}/clusters/{id}/approve:
    post:
      operationId: approveCluster
      tags: [clusters]
      parameters:
        - name: projectId
          in: path
          required: true
          schema:
            type: integer
        - name: id
          in: path
          required: true
          schema:
            type: integer
      responses:
        "200":
          description: Approved cluster
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/KeywordCluster"

  /projects/{projectId}/clusters/{id}/reject:
    post:
      operationId: rejectCluster
      tags: [clusters]
      parameters:
        - name: projectId
          in: path
          required: true
          schema:
            type: integer
        - name: id
          in: path
          required: true
          schema:
            type: integer
      responses:
        "200":
          description: Rejected cluster
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/KeywordCluster"

  /projects/{projectId}/topic-map:
    get:
      operationId: getTopicMap
      tags: [clusters]
      parameters:
        - name: projectId
          in: path
          required: true
          schema:
            type: integer
      responses:
        "200":
          description: Topic map
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/TopicMap"

  /projects/{projectId}/roadmap:
    get:
      operationId: getContentRoadmap
      tags: [clusters]
      parameters:
        - name: projectId
          in: path
          required: true
          schema:
            type: integer
      responses:
        "200":
          description: Content roadmap items
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/RoadmapItem"

  /ai-tasks:
    get:
      operationId: listAiTasks
      tags: [ai-tasks]
      parameters:
        - name: projectId
          in: query
          required: false
          schema:
            type: integer
        - name: status
          in: query
          required: false
          schema:
            type: string
      responses:
        "200":
          description: List of AI tasks
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/AiTask"

  /ai-tasks/{id}:
    get:
      operationId: getAiTask
      tags: [ai-tasks]
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
      responses:
        "200":
          description: AI task
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AiTask"
        "404":
          description: Not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"

  /projects/{projectId}/briefs:
    get:
      operationId: listBriefs
      tags: [briefs]
      parameters:
        - name: projectId
          in: path
          required: true
          schema:
            type: integer
        - name: status
          in: query
          required: false
          schema:
            type: string
      responses:
        "200":
          description: List of briefs
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/ContentBrief"
    post:
      operationId: createBrief
      tags: [briefs]
      parameters:
        - name: projectId
          in: path
          required: true
          schema:
            type: integer
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CreateBriefBody"
      responses:
        "201":
          description: Created brief
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ContentBrief"

  /projects/{projectId}/briefs/{id}:
    get:
      operationId: getBrief
      tags: [briefs]
      parameters:
        - name: projectId
          in: path
          required: true
          schema:
            type: integer
        - name: id
          in: path
          required: true
          schema:
            type: integer
      responses:
        "200":
          description: Brief
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ContentBrief"
        "404":
          description: Not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
    patch:
      operationId: updateBrief
      tags: [briefs]
      parameters:
        - name: projectId
          in: path
          required: true
          schema:
            type: integer
        - name: id
          in: path
          required: true
          schema:
            type: integer
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/UpdateBriefBody"
      responses:
        "200":
          description: Updated brief
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ContentBrief"
    delete:
      operationId: deleteBrief
      tags: [briefs]
      parameters:
        - name: projectId
          in: path
          required: true
          schema:
            type: integer
        - name: id
          in: path
          required: true
          schema:
            type: integer
      responses:
        "204":
          description: Deleted

  /projects/{projectId}/briefs/{id}/generate:
    post:
      operationId: generateBrief
      tags: [briefs]
      parameters:
        - name: projectId
          in: path
          required: true
          schema:
            type: integer
        - name: id
          in: path
          required: true
          schema:
            type: integer
      responses:
        "202":
          description: Generation task queued
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AiTask"

  /projects/{projectId}/briefs/{id}/approve:
    post:
      operationId: approveBrief
      tags: [briefs]
      parameters:
        - name: projectId
          in: path
          required: true
          schema:
            type: integer
        - name: id
          in: path
          required: true
          schema:
            type: integer
      responses:
        "200":
          description: Approved brief
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ContentBrief"

  /projects/{projectId}/reports:
    get:
      operationId: listReports
      tags: [reports]
      parameters:
        - name: projectId
          in: path
          required: true
          schema:
            type: integer
      responses:
        "200":
          description: List of reports
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/Report"
    post:
      operationId: generateReport
      tags: [reports]
      parameters:
        - name: projectId
          in: path
          required: true
          schema:
            type: integer
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/GenerateReportBody"
      responses:
        "201":
          description: Generated report
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Report"

  /projects/{projectId}/reports/{id}:
    get:
      operationId: getReport
      tags: [reports]
      parameters:
        - name: projectId
          in: path
          required: true
          schema:
            type: integer
        - name: id
          in: path
          required: true
          schema:
            type: integer
      responses:
        "200":
          description: Report
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Report"
        "404":
          description: Not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
    delete:
      operationId: deleteReport
      tags: [reports]
      parameters:
        - name: projectId
          in: path
          required: true
          schema:
            type: integer
        - name: id
          in: path
          required: true
          schema:
            type: integer
      responses:
        "204":
          description: Deleted

  /geo-aeo/audits:
    get:
      operationId: listGeoAeoAudits
      tags: [geo-aeo]
      parameters:
        - name: clientId
          in: query
          schema:
            type: integer
        - name: projectId
          in: query
          schema:
            type: integer
        - name: status
          in: query
          schema:
            type: string
      responses:
        "200":
          description: GEO/AEO audits
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/GeoAeoAudit"
    post:
      operationId: createGeoAeoAudit
      tags: [geo-aeo]
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/GeoAeoAuditCreateBody"
      responses:
        "201":
          description: Created GEO/AEO audit
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/GeoAeoAudit"

  /geo-aeo/client/audits:
    get:
      operationId: listApprovedGeoAeoClientAudits
      tags: [geo-aeo]
      responses:
        "200":
          description: Approved GEO/AEO audits visible to client viewers
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/GeoAeoAudit"
        "403":
          description: GEO/AEO client dashboard denied by role or license
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"

  /geo-aeo/client/audits/{auditId}:
    get:
      operationId: getApprovedGeoAeoClientAudit
      tags: [geo-aeo]
      parameters:
        - $ref: "#/components/parameters/GeoAeoAuditId"
      responses:
        "200":
          description: Approved GEO/AEO audit detail visible to client viewers
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/GeoAeoClientAuditDetail"
        "404":
          description: Audit not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "403":
          description: GEO/AEO client dashboard denied by role or license
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"

  /geo-aeo/client/audits/{auditId}/monitoring-runs:
    get:
      operationId: listApprovedGeoAeoClientMonitoringRuns
      tags: [geo-aeo]
      parameters:
        - $ref: "#/components/parameters/GeoAeoAuditId"
      responses:
        "200":
          description: Approved monthly GEO/AEO monitoring progress visible to client viewers
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/GeoAeoMonitoringRun"
        "404":
          description: Audit not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "403":
          description: GEO/AEO client dashboard denied by role or license
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"

  /geo-aeo/audits/{auditId}:
    get:
      operationId: getGeoAeoAudit
      tags: [geo-aeo]
      parameters:
        - $ref: "#/components/parameters/GeoAeoAuditId"
      responses:
        "200":
          description: GEO/AEO audit
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/GeoAeoAudit"
    patch:
      operationId: updateGeoAeoAudit
      tags: [geo-aeo]
      parameters:
        - $ref: "#/components/parameters/GeoAeoAuditId"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/GeoAeoAuditUpdateBody"
      responses:
        "200":
          description: Updated GEO/AEO audit
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/GeoAeoAudit"
    delete:
      operationId: deleteGeoAeoAudit
      tags: [geo-aeo]
      parameters:
        - $ref: "#/components/parameters/GeoAeoAuditId"
      responses:
        "204":
          description: Deleted

  /geo-aeo/audits/{auditId}/approve:
    post:
      operationId: approveGeoAeoAudit
      tags: [geo-aeo]
      parameters:
        - $ref: "#/components/parameters/GeoAeoAuditId"
      responses:
        "200":
          description: Approved GEO/AEO audit
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/GeoAeoAudit"

  /geo-aeo/audits/{auditId}/prompts:
    get:
      operationId: listGeoAeoPrompts
      tags: [geo-aeo]
      parameters:
        - $ref: "#/components/parameters/GeoAeoAuditId"
      responses:
        "200":
          description: GEO/AEO prompts
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/GeoAeoPrompt"
    post:
      operationId: createGeoAeoPrompt
      tags: [geo-aeo]
      parameters:
        - $ref: "#/components/parameters/GeoAeoAuditId"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/GeoAeoPromptCreateBody"
      responses:
        "201":
          description: Created prompt
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/GeoAeoPrompt"

  /geo-aeo/audits/{auditId}/prompts/import:
    post:
      operationId: importGeoAeoPrompts
      tags: [geo-aeo]
      parameters:
        - $ref: "#/components/parameters/GeoAeoAuditId"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/GeoAeoCsvImportBody"
      responses:
        "201":
          description: Imported prompts
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/GeoAeoPromptImportResult"

  /geo-aeo/audits/{auditId}/prompts/import/preview:
    post:
      operationId: previewGeoAeoPromptsImport
      tags: [geo-aeo]
      parameters:
        - $ref: "#/components/parameters/GeoAeoAuditId"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/GeoAeoCsvImportBody"
      responses:
        "200":
          description: Previewed prompt CSV import rows
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/GeoAeoCsvImportPreview"

  /geo-aeo/audits/{auditId}/snapshots:
    get:
      operationId: listGeoAeoAnswerSnapshots
      tags: [geo-aeo]
      parameters:
        - $ref: "#/components/parameters/GeoAeoAuditId"
      responses:
        "200":
          description: GEO/AEO answer snapshots
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/GeoAeoAnswerSnapshot"
    post:
      operationId: createGeoAeoAnswerSnapshot
      tags: [geo-aeo]
      parameters:
        - $ref: "#/components/parameters/GeoAeoAuditId"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/GeoAeoSnapshotCreateBody"
      responses:
        "201":
          description: Created answer snapshot
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/GeoAeoAnswerSnapshot"

  /geo-aeo/audits/{auditId}/snapshots/import-csv:
    post:
      operationId: importGeoAeoAnswerSnapshots
      tags: [geo-aeo]
      parameters:
        - $ref: "#/components/parameters/GeoAeoAuditId"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/GeoAeoCsvImportBody"
      responses:
        "201":
          description: Imported answer snapshots
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/GeoAeoSnapshotImportResult"

  /geo-aeo/audits/{auditId}/snapshots/import-csv/preview:
    post:
      operationId: previewGeoAeoAnswerSnapshotsImport
      tags: [geo-aeo]
      parameters:
        - $ref: "#/components/parameters/GeoAeoAuditId"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/GeoAeoCsvImportBody"
      responses:
        "200":
          description: Previewed answer snapshot CSV import rows
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/GeoAeoCsvImportPreview"

  /geo-aeo/audits/{auditId}/snapshot-imports:
    get:
      operationId: listGeoAeoSnapshotImportBatches
      tags: [geo-aeo]
      parameters:
        - $ref: "#/components/parameters/GeoAeoAuditId"
      responses:
        "200":
          description: Snapshot CSV import batches
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/GeoAeoSnapshotImportBatch"

  /geo-aeo/snapshot-imports/{importBatchId}:
    delete:
      operationId: rollbackGeoAeoSnapshotImportBatch
      tags: [geo-aeo]
      parameters:
        - name: importBatchId
          in: path
          required: true
          schema:
            type: integer
      responses:
        "200":
          description: Rolled back snapshot CSV import batch
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/GeoAeoSnapshotImportRollbackResult"

  /geo-aeo/snapshots/{snapshotId}:
    patch:
      operationId: updateGeoAeoAnswerSnapshot
      tags: [geo-aeo]
      parameters:
        - name: snapshotId
          in: path
          required: true
          schema:
            type: integer
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/GeoAeoSnapshotUpdateBody"
      responses:
        "200":
          description: Updated answer snapshot
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/GeoAeoAnswerSnapshot"

  /geo-aeo/audits/{auditId}/competitors:
    get:
      operationId: listGeoAeoCompetitors
      tags: [geo-aeo]
      parameters:
        - $ref: "#/components/parameters/GeoAeoAuditId"
      responses:
        "200":
          description: GEO/AEO competitors
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/GeoAeoManualRecord"
    post:
      operationId: createGeoAeoCompetitor
      tags: [geo-aeo]
      parameters:
        - $ref: "#/components/parameters/GeoAeoAuditId"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/GeoAeoCompetitorCreateBody"
      responses:
        "201":
          description: Created competitor
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/GeoAeoManualRecord"

  /geo-aeo/competitors/{competitorId}:
    patch:
      operationId: updateGeoAeoCompetitor
      tags: [geo-aeo]
      parameters:
        - name: competitorId
          in: path
          required: true
          schema:
            type: integer
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/GeoAeoCompetitorUpdateBody"
      responses:
        "200":
          description: Updated competitor
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/GeoAeoManualRecord"
    delete:
      operationId: deleteGeoAeoCompetitor
      tags: [geo-aeo]
      parameters:
        - name: competitorId
          in: path
          required: true
          schema:
            type: integer
      responses:
        "204":
          description: Deleted competitor

  /geo-aeo/audits/{auditId}/citations:
    get:
      operationId: listGeoAeoCitations
      tags: [geo-aeo]
      parameters:
        - $ref: "#/components/parameters/GeoAeoAuditId"
      responses:
        "200":
          description: GEO/AEO citations
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/GeoAeoManualRecord"
    post:
      operationId: createGeoAeoCitation
      tags: [geo-aeo]
      parameters:
        - $ref: "#/components/parameters/GeoAeoAuditId"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/GeoAeoCitationCreateBody"
      responses:
        "201":
          description: Created citation
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/GeoAeoManualRecord"

  /geo-aeo/citations/{citationId}:
    delete:
      operationId: deleteGeoAeoCitation
      tags: [geo-aeo]
      parameters:
        - name: citationId
          in: path
          required: true
          schema:
            type: integer
      responses:
        "204":
          description: Deleted

  /geo-aeo/audits/{auditId}/source-recommendations:
    get:
      operationId: listGeoAeoSourceRecommendations
      tags: [geo-aeo]
      parameters:
        - $ref: "#/components/parameters/GeoAeoAuditId"
      responses:
        "200":
          description: GEO/AEO source recommendations
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/GeoAeoManualRecord"
    post:
      operationId: createGeoAeoSourceRecommendation
      tags: [geo-aeo]
      parameters:
        - $ref: "#/components/parameters/GeoAeoAuditId"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/GeoAeoSourceRecommendationCreateBody"
      responses:
        "201":
          description: Created source recommendation
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/GeoAeoManualRecord"

  /geo-aeo/source-recommendations/{sourceRecommendationId}:
    patch:
      operationId: updateGeoAeoSourceRecommendation
      tags: [geo-aeo]
      parameters:
        - name: sourceRecommendationId
          in: path
          required: true
          schema:
            type: integer
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/GeoAeoSourceRecommendationUpdateBody"
      responses:
        "200":
          description: Updated source recommendation
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/GeoAeoManualRecord"
    delete:
      operationId: deleteGeoAeoSourceRecommendation
      tags: [geo-aeo]
      parameters:
        - name: sourceRecommendationId
          in: path
          required: true
          schema:
            type: integer
      responses:
        "204":
          description: Deleted source recommendation

  /geo-aeo/audits/{auditId}/schema-findings:
    get:
      operationId: listGeoAeoSchemaFindings
      tags: [geo-aeo]
      parameters:
        - $ref: "#/components/parameters/GeoAeoAuditId"
      responses:
        "200":
          description: GEO/AEO schema findings
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/GeoAeoManualRecord"
    post:
      operationId: createGeoAeoSchemaFinding
      tags: [geo-aeo]
      parameters:
        - $ref: "#/components/parameters/GeoAeoAuditId"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/GeoAeoSchemaFindingCreateBody"
      responses:
        "201":
          description: Created schema finding
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/GeoAeoManualRecord"

  /geo-aeo/schema-findings/{schemaFindingId}:
    patch:
      operationId: updateGeoAeoSchemaFinding
      tags: [geo-aeo]
      parameters:
        - name: schemaFindingId
          in: path
          required: true
          schema:
            type: integer
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/GeoAeoSchemaFindingUpdateBody"
      responses:
        "200":
          description: Updated schema finding
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/GeoAeoManualRecord"
    delete:
      operationId: deleteGeoAeoSchemaFinding
      tags: [geo-aeo]
      parameters:
        - name: schemaFindingId
          in: path
          required: true
          schema:
            type: integer
      responses:
        "204":
          description: Deleted schema finding

  /geo-aeo/audits/{auditId}/monitoring-runs:
    get:
      operationId: listGeoAeoMonitoringRuns
      tags: [geo-aeo]
      parameters:
        - $ref: "#/components/parameters/GeoAeoAuditId"
      responses:
        "200":
          description: GEO/AEO monitoring runs
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/GeoAeoMonitoringRun"
    post:
      operationId: createGeoAeoMonitoringRun
      tags: [geo-aeo]
      parameters:
        - $ref: "#/components/parameters/GeoAeoAuditId"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/GeoAeoMonitoringRunCreateBody"
      responses:
        "201":
          description: Created monitoring run
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/GeoAeoMonitoringRun"

  /geo-aeo/monitoring-runs/{monitoringRunId}:
    patch:
      operationId: updateGeoAeoMonitoringRun
      tags: [geo-aeo]
      parameters:
        - name: monitoringRunId
          in: path
          required: true
          schema:
            type: integer
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/GeoAeoMonitoringRunUpdateBody"
      responses:
        "200":
          description: Updated monitoring run
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/GeoAeoMonitoringRun"

  /geo-aeo/monitoring-runs/{monitoringRunId}/approve:
    post:
      operationId: approveGeoAeoMonitoringRun
      tags: [geo-aeo]
      parameters:
        - name: monitoringRunId
          in: path
          required: true
          schema:
            type: integer
      responses:
        "200":
          description: Approved monitoring run
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/GeoAeoMonitoringRun"

  /geo-aeo/audits/{auditId}/analyze:
    post:
      operationId: analyzeGeoAeoAudit
      tags: [geo-aeo]
      parameters:
        - $ref: "#/components/parameters/GeoAeoAuditId"
      responses:
        "200":
          description: GEO/AEO analysis result
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/GeoAeoAnalysisResult"

  /geo-aeo/audits/{auditId}/score:
    post:
      operationId: scoreGeoAeoAudit
      tags: [geo-aeo]
      parameters:
        - $ref: "#/components/parameters/GeoAeoAuditId"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/GeoAeoScoreBody"
      responses:
        "201":
          description: Stored GEO/AEO score
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/GeoAeoStoredScoreResult"

  /geo-aeo/audits/{auditId}/findings:
    get:
      operationId: listGeoAeoFindings
      tags: [geo-aeo]
      parameters:
        - $ref: "#/components/parameters/GeoAeoAuditId"
      responses:
        "200":
          description: GEO/AEO findings
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/GeoAeoFinding"

  /geo-aeo/findings/{findingId}:
    patch:
      operationId: updateGeoAeoFinding
      tags: [geo-aeo]
      parameters:
        - name: findingId
          in: path
          required: true
          schema:
            type: integer
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/GeoAeoFindingUpdateBody"
      responses:
        "200":
          description: Updated finding
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/GeoAeoFinding"

  /geo-aeo/audits/{auditId}/action-plan/generate:
    post:
      operationId: generateGeoAeoActionPlan
      tags: [geo-aeo]
      parameters:
        - $ref: "#/components/parameters/GeoAeoAuditId"
      requestBody:
        required: false
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/GeoAeoActionPlanGenerateBody"
      responses:
        "201":
          description: Generated action plan
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/GeoAeoActionPlanWithItems"

  /geo-aeo/audits/{auditId}/action-plan:
    get:
      operationId: getGeoAeoActionPlan
      tags: [geo-aeo]
      parameters:
        - $ref: "#/components/parameters/GeoAeoAuditId"
      responses:
        "200":
          description: Latest action plan
          content:
            application/json:
              schema:
                oneOf:
                  - $ref: "#/components/schemas/GeoAeoActionPlanWithItems"
                  - type: "null"

  /geo-aeo/audits/{auditId}/action-items:
    post:
      operationId: createGeoAeoActionItem
      tags: [geo-aeo]
      parameters:
        - $ref: "#/components/parameters/GeoAeoAuditId"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/GeoAeoActionItemCreateBody"
      responses:
        "201":
          description: Created manual action item
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/GeoAeoActionPlanItemResult"

  /geo-aeo/action-items/{actionItemId}:
    patch:
      operationId: updateGeoAeoActionItem
      tags: [geo-aeo]
      parameters:
        - name: actionItemId
          in: path
          required: true
          schema:
            type: integer
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/GeoAeoActionItemUpdateBody"
      responses:
        "200":
          description: Updated action item
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/GeoAeoActionItem"
    delete:
      operationId: deleteGeoAeoActionItem
      tags: [geo-aeo]
      parameters:
        - name: actionItemId
          in: path
          required: true
          schema:
            type: integer
      responses:
        "204":
          description: Deleted action item

  /geo-aeo/audits/{auditId}/report/generate:
    post:
      operationId: generateGeoAeoReport
      tags: [geo-aeo]
      parameters:
        - $ref: "#/components/parameters/GeoAeoAuditId"
      requestBody:
        required: false
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/GeoAeoReportGenerateBody"
      responses:
        "201":
          description: Generated GEO/AEO report
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/GeoAeoReport"
        "409":
          description: Audit requires a linked project
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"

  /geo-aeo/reports/{reportId}/export:
    post:
      operationId: exportGeoAeoReport
      tags: [geo-aeo]
      parameters:
        - name: reportId
          in: path
          required: true
          schema:
            type: integer
      requestBody:
        required: false
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/GeoAeoReportExportBody"
      responses:
        "200":
          description: Exported GEO/AEO report
          content:
            text/markdown:
              schema:
                type: string
            text/csv:
              schema:
                type: string
            application/pdf:
              schema:
                type: string
                format: binary
            application/json:
              schema:
                type: object
                additionalProperties: true
        "403":
          description: Export denied by role, geoAeo.exportReports permission, approval, or download license
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "404":
          description: GEO/AEO report not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"

  /billing/plans:
    get:
      operationId: listPlans
      tags: [billing]
      responses:
        "200":
          description: Available plans
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/Plan"

  /billing/subscription:
    get:
      operationId: getSubscription
      tags: [billing]
      responses:
        "200":
          description: Subscription info
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Subscription"

  /billing/checkout:
    post:
      operationId: createCheckoutSession
      tags: [billing]
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CreateCheckoutBody"
      responses:
        "200":
          description: Checkout URL
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/CheckoutResponse"

  /billing/portal:
    post:
      operationId: createBillingPortal
      tags: [billing]
      responses:
        "200":
          description: Billing portal URL
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/CheckoutResponse"

  /billing/usage:
    get:
      operationId: getBillingUsage
      tags: [billing]
      responses:
        "200":
          description: Usage summary
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/UsageSummary"

  /team:
    get:
      operationId: listTeamMembers
      tags: [team]
      responses:
        "200":
          description: Team members
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/TeamMember"

  /team/{userId}:
    patch:
      operationId: updateTeamMemberRole
      tags: [team]
      parameters:
        - name: userId
          in: path
          required: true
          schema:
            type: integer
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/UpdateTeamMemberRoleBody"
      responses:
        "200":
          description: Updated team member
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/User"
        "400":
          description: Invalid role
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "404":
          description: Team member not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
    delete:
      operationId: removeTeamMember
      tags: [team]
      parameters:
        - name: userId
          in: path
          required: true
          schema:
            type: integer
      responses:
        "204":
          description: Removed
        "400":
          description: Cannot remove current user
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "404":
          description: Team member not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"

  /team/invite:
    post:
      operationId: inviteTeamMember
      tags: [team]
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/InviteTeamMemberBody"
      responses:
        "201":
          description: Invitation created
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/TeamInviteResponse"
        "400":
          description: Invalid invite
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "402":
          description: Seat limit reached
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "409":
          description: User already exists
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"

  /team/invitations:
    get:
      operationId: listTeamInvitations
      tags: [team]
      responses:
        "200":
          description: Team invitations
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/TeamInvitation"

  /team/invitations/{id}:
    delete:
      operationId: deleteTeamInvitation
      tags: [team]
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
      responses:
        "204":
          description: Deleted

  /team/invitations/accept:
    post:
      operationId: acceptTeamInvitation
      tags: [team]
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/AcceptTeamInvitationBody"
      responses:
        "200":
          description: Invitation accepted
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AcceptTeamInvitationResponse"
        "400":
          description: Invalid request
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "404":
          description: Invitation not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "409":
          description: Invitation already accepted
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "410":
          description: Invitation expired
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"

  /integrations:
    get:
      operationId: listIntegrations
      tags: [integrations]
      responses:
        "200":
          description: Configured integrations
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/Integration"
    post:
      operationId: configureIntegration
      tags: [integrations]
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/ConfigureIntegrationBody"
      responses:
        "200":
          description: Updated integration
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Integration"
        "201":
          description: Created integration
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Integration"
        "400":
          description: Invalid integration payload
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"

  /integrations/{provider}:
    delete:
      operationId: deleteIntegration
      tags: [integrations]
      parameters:
        - name: provider
          in: path
          required: true
          schema:
            $ref: "#/components/schemas/IntegrationProvider"
      responses:
        "204":
          description: Deleted

  /integrations/{provider}/search:
    post:
      operationId: searchIntegrationKeywords
      tags: [integrations]
      parameters:
        - name: provider
          in: path
          required: true
          schema:
            $ref: "#/components/schemas/KeywordSearchProvider"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/SearchIntegrationKeywordsBody"
      responses:
        "200":
          description: Keyword provider results
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/ProviderKeyword"
        "400":
          description: Invalid provider or query
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "500":
          description: Stored credentials could not be read
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"

  /api-keys:
    get:
      operationId: listApiKeys
      tags: [api-keys]
      responses:
        "200":
          description: Active API keys
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/ApiKey"
    post:
      operationId: createApiKey
      tags: [api-keys]
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CreateApiKeyBody"
      responses:
        "201":
          description: Created API key with one-time secret
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/CreateApiKeyResponse"
        "400":
          description: Invalid API key payload
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"

  /api-keys/{id}:
    delete:
      operationId: revokeApiKey
      tags: [api-keys]
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
      responses:
        "204":
          description: Revoked
        "404":
          description: API key not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"

  /webhooks:
    get:
      operationId: listWebhooks
      tags: [webhooks]
      responses:
        "200":
          description: Webhook endpoints
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/WebhookEndpoint"
    post:
      operationId: createWebhook
      tags: [webhooks]
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CreateWebhookBody"
      responses:
        "201":
          description: Created webhook endpoint
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/WebhookEndpointWithSecret"
        "400":
          description: Invalid webhook payload
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"

  /webhooks/{id}:
    patch:
      operationId: updateWebhook
      tags: [webhooks]
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/UpdateWebhookBody"
      responses:
        "200":
          description: Updated webhook endpoint
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/WebhookEndpoint"
        "400":
          description: Invalid webhook URL
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "404":
          description: Webhook not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
    delete:
      operationId: deleteWebhook
      tags: [webhooks]
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
      responses:
        "204":
          description: Deleted

  /webhooks/{id}/test:
    post:
      operationId: testWebhook
      tags: [webhooks]
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
      responses:
        "200":
          description: Test event queued
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/MessageResponse"
        "404":
          description: Webhook not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"

  /webhooks/{id}/deliveries:
    get:
      operationId: listWebhookDeliveries
      tags: [webhooks]
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
        - name: limit
          in: query
          required: false
          schema:
            type: integer
            maximum: 100
      responses:
        "200":
          description: Webhook deliveries
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/WebhookDelivery"

  /webhooks/events:
    get:
      operationId: listWebhookEvents
      tags: [webhooks]
      responses:
        "200":
          description: Supported webhook events
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/WebhookEvent"

  /notifications:
    get:
      operationId: listNotifications
      tags: [notifications]
      responses:
        "200":
          description: Notifications
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/Notification"

  /notifications/unread-count:
    get:
      operationId: getUnreadNotificationCount
      tags: [notifications]
      responses:
        "200":
          description: Unread notification count
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/UnreadNotificationCount"

  /notifications/read-all:
    patch:
      operationId: markAllNotificationsRead
      tags: [notifications]
      responses:
        "200":
          description: All unread notifications marked read
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/OkResponse"

  /notifications/{id}/read:
    patch:
      operationId: markNotificationRead
      tags: [notifications]
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
      responses:
        "200":
          description: Notification marked read
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/OkResponse"
        "400":
          description: Invalid notification id
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"

  /notifications/{id}:
    delete:
      operationId: deleteNotification
      tags: [notifications]
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
      responses:
        "200":
          description: Notification deleted
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/OkResponse"
        "400":
          description: Invalid notification id
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"

  /healthz/detailed:
    get:
      operationId: getDetailedHealth
      tags: [health]
      parameters:
        - name: x-health-check-token
          in: header
          required: false
          schema:
            type: string
        - name: authorization
          in: header
          required: false
          schema:
            type: string
      responses:
        "200":
          description: Detailed operational health
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/DetailedHealthStatus"
        "503":
          description: One or more required services are degraded
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/DetailedHealthStatus"

  /tenant:
    get:
      operationId: getTenant
      tags: [tenant]
      responses:
        "200":
          description: Current tenant
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Tenant"
        "404":
          description: Tenant not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
    patch:
      operationId: updateTenant
      tags: [tenant]
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/UpdateTenantBody"
      responses:
        "200":
          description: Updated tenant
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Tenant"
        "400":
          description: Invalid tenant payload
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"

  /tenant/dashboard:
    get:
      operationId: getTenantDashboard
      tags: [tenant]
      responses:
        "200":
          description: Tenant dashboard summary
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/DashboardSummary"

  /clients/{clientId}/projects:
    get:
      operationId: listClientProjects
      tags: [projects]
      parameters:
        - name: clientId
          in: path
          required: true
          schema:
            type: integer
      responses:
        "200":
          description: Projects for a client
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/Project"
        "404":
          description: Client not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
    post:
      operationId: createClientProject
      tags: [projects]
      parameters:
        - name: clientId
          in: path
          required: true
          schema:
            type: integer
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CreateProjectBody"
      responses:
        "201":
          description: Created project
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Project"
        "400":
          description: Invalid project payload
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "404":
          description: Client not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"

  /clients/{clientId}/projects/{id}:
    get:
      operationId: getClientProject
      tags: [projects]
      parameters:
        - name: clientId
          in: path
          required: true
          schema:
            type: integer
        - name: id
          in: path
          required: true
          schema:
            type: integer
      responses:
        "200":
          description: Project scoped to a client
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Project"
        "404":
          description: Project not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
    patch:
      operationId: updateClientProject
      tags: [projects]
      parameters:
        - name: clientId
          in: path
          required: true
          schema:
            type: integer
        - name: id
          in: path
          required: true
          schema:
            type: integer
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/UpdateProjectBody"
      responses:
        "200":
          description: Updated project
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Project"
        "400":
          description: Invalid project payload
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "404":
          description: Project not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
    delete:
      operationId: deleteClientProject
      tags: [projects]
      parameters:
        - name: clientId
          in: path
          required: true
          schema:
            type: integer
        - name: id
          in: path
          required: true
          schema:
            type: integer
      responses:
        "204":
          description: Deleted

  /analytics/overview:
    get:
      operationId: getAnalyticsOverview
      tags: [analytics]
      responses:
        "200":
          description: Tenant-wide analytics overview
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AnalyticsOverview"

  /analytics/projects:
    get:
      operationId: listAnalyticsProjects
      tags: [analytics]
      responses:
        "200":
          description: Per-project analytics rollups
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/AnalyticsProject"

  /analytics/velocity:
    get:
      operationId: getAnalyticsVelocity
      tags: [analytics]
      responses:
        "200":
          description: Daily content velocity counts
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/AnalyticsVelocityPoint"

  /audit-log:
    get:
      operationId: listAuditLogEntries
      tags: [audit-log]
      parameters:
        - name: limit
          in: query
          required: false
          schema:
            type: integer
            maximum: 200
        - name: offset
          in: query
          required: false
          schema:
            type: integer
        - name: resourceType
          in: query
          required: false
          schema:
            type: string
        - name: action
          in: query
          required: false
          schema:
            type: string
      responses:
        "200":
          description: Audit log entries
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/AuditLogEntry"

  /comments:
    get:
      operationId: listComments
      tags: [comments]
      parameters:
        - name: entityType
          in: query
          required: true
          schema:
            $ref: "#/components/schemas/CommentEntityType"
        - name: entityId
          in: query
          required: true
          schema:
            type: integer
      responses:
        "200":
          description: Comments for an entity
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/Comment"
        "400":
          description: Invalid comment query
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
    post:
      operationId: createComment
      tags: [comments]
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CreateCommentBody"
      responses:
        "201":
          description: Created comment
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Comment"
        "400":
          description: Invalid comment payload
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"

  /comments/{id}/resolve:
    patch:
      operationId: resolveComment
      tags: [comments]
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
      responses:
        "200":
          description: Resolved comment
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Comment"
        "404":
          description: Comment not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"

  /comments/{id}:
    delete:
      operationId: deleteComment
      tags: [comments]
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
      responses:
        "200":
          description: Deleted comment
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/OkResponse"
        "403":
          description: Cannot delete another user's comment
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"

  /custom-fields:
    get:
      operationId: listCustomFields
      tags: [custom-fields]
      parameters:
        - name: entityType
          in: query
          required: false
          schema:
            $ref: "#/components/schemas/CustomFieldEntityType"
      responses:
        "200":
          description: Custom field definitions
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/CustomField"
    post:
      operationId: createCustomField
      tags: [custom-fields]
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CustomFieldBody"
      responses:
        "201":
          description: Created custom field definition
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/CustomField"
        "400":
          description: Invalid custom field payload
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"

  /custom-fields/{id}:
    delete:
      operationId: deleteCustomField
      tags: [custom-fields]
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
      responses:
        "200":
          description: Deleted custom field
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/OkResponse"

  /custom-field-values:
    get:
      operationId: listCustomFieldValues
      tags: [custom-fields]
      parameters:
        - name: entityType
          in: query
          required: true
          schema:
            $ref: "#/components/schemas/CustomFieldEntityType"
        - name: entityId
          in: query
          required: true
          schema:
            type: integer
      responses:
        "200":
          description: Custom field values for an entity
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/CustomFieldValue"
        "400":
          description: Invalid custom field value query
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
    put:
      operationId: upsertCustomFieldValue
      tags: [custom-fields]
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/UpsertCustomFieldValueBody"
      responses:
        "200":
          description: Updated custom field value
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/CustomFieldValue"
        "201":
          description: Created custom field value
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/CustomFieldValue"
        "400":
          description: Invalid custom field value payload
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"

  /gdpr/export:
    get:
      operationId: exportGdprData
      tags: [gdpr]
      responses:
        "200":
          description: JSON export for the current user's GDPR right of access
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/GdprExport"

  /gdpr/me:
    delete:
      operationId: deleteCurrentUserGdprData
      tags: [gdpr]
      responses:
        "200":
          description: Account deletion accepted
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/GdprDeleteResponse"
        "422":
          description: Admin ownership transfer required first
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"

  /projects/{projectId}/calendar:
    get:
      operationId: listProjectCalendarEntries
      tags: [calendar]
      parameters:
        - name: projectId
          in: path
          required: true
          schema:
            type: integer
      responses:
        "200":
          description: Content calendar entries
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/ContentCalendarEntry"
    post:
      operationId: createProjectCalendarEntry
      tags: [calendar]
      parameters:
        - name: projectId
          in: path
          required: true
          schema:
            type: integer
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CalendarEntryBody"
      responses:
        "201":
          description: Created calendar entry
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ContentCalendarEntry"
        "400":
          description: Invalid calendar payload
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"

  /projects/{projectId}/calendar/{id}:
    patch:
      operationId: updateProjectCalendarEntry
      tags: [calendar]
      parameters:
        - name: projectId
          in: path
          required: true
          schema:
            type: integer
        - name: id
          in: path
          required: true
          schema:
            type: integer
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/UpdateCalendarEntryBody"
      responses:
        "200":
          description: Updated calendar entry
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ContentCalendarEntry"
        "404":
          description: Calendar entry not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
    delete:
      operationId: deleteProjectCalendarEntry
      tags: [calendar]
      parameters:
        - name: projectId
          in: path
          required: true
          schema:
            type: integer
        - name: id
          in: path
          required: true
          schema:
            type: integer
      responses:
        "200":
          description: Deleted calendar entry
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/OkResponse"

  /projects/{projectId}/competitors:
    get:
      operationId: listProjectCompetitors
      tags: [competitors]
      parameters:
        - name: projectId
          in: path
          required: true
          schema:
            type: integer
      responses:
        "200":
          description: Competitor domains
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/CompetitorDomain"
    post:
      operationId: createProjectCompetitor
      tags: [competitors]
      parameters:
        - name: projectId
          in: path
          required: true
          schema:
            type: integer
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CreateCompetitorBody"
      responses:
        "201":
          description: Created competitor domain
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/CompetitorDomain"
        "400":
          description: Invalid competitor payload
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"

  /projects/{projectId}/competitors/{id}:
    delete:
      operationId: deleteProjectCompetitor
      tags: [competitors]
      parameters:
        - name: projectId
          in: path
          required: true
          schema:
            type: integer
        - name: id
          in: path
          required: true
          schema:
            type: integer
      responses:
        "200":
          description: Deleted competitor domain
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/OkResponse"

  /projects/{projectId}/competitors/keyword-gap:
    get:
      operationId: getKeywordGap
      tags: [competitors]
      parameters:
        - name: projectId
          in: path
          required: true
          schema:
            type: integer
      responses:
        "200":
          description: Competitor keyword gap analysis
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/KeywordGapResponse"

  /projects/{projectId}/export/keywords.csv:
    get:
      operationId: exportProjectKeywordsCsv
      tags: [exports]
      parameters:
        - name: projectId
          in: path
          required: true
          schema:
            type: integer
      responses:
        "200":
          description: Project keywords as CSV
          content:
            text/csv:
              schema:
                type: string
        "404":
          description: Project not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"

  /projects/{projectId}/export/project.json:
    get:
      operationId: exportProjectJson
      tags: [exports]
      parameters:
        - name: projectId
          in: path
          required: true
          schema:
            type: integer
      responses:
        "200":
          description: Full project export
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ProjectExport"
        "404":
          description: Project not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"

  /projects/{projectId}/rankings:
    get:
      operationId: listProjectRankings
      tags: [rankings]
      parameters:
        - name: projectId
          in: path
          required: true
          schema:
            type: integer
      responses:
        "200":
          description: Latest ranking summary per keyword
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/KeywordRanking"

  /projects/{projectId}/rankings/{keywordId}/history:
    get:
      operationId: getProjectRankingHistory
      tags: [rankings]
      parameters:
        - name: projectId
          in: path
          required: true
          schema:
            type: integer
        - name: keywordId
          in: path
          required: true
          schema:
            type: integer
      responses:
        "200":
          description: Ranking history for a keyword
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/KeywordRankingHistoryEntry"

  /projects/{projectId}/rankings/check:
    post:
      operationId: checkProjectRanking
      tags: [rankings]
      parameters:
        - name: projectId
          in: path
          required: true
          schema:
            type: integer
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CheckRankingBody"
      responses:
        "201":
          description: Recorded ranking check
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/KeywordRankingHistoryEntry"

  /projects/{projectId}/rankings/check-all:
    post:
      operationId: checkAllProjectRankings
      tags: [rankings]
      parameters:
        - name: projectId
          in: path
          required: true
          schema:
            type: integer
      responses:
        "200":
          description: Bulk ranking check summary
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BulkRankingCheckResponse"

  /projects/{projectId}/clusters/auto:
    post:
      operationId: autoClusterProjectKeywords
      tags: [clusters]
      parameters:
        - name: projectId
          in: path
          required: true
          schema:
            type: integer
      responses:
        "201":
          description: Auto-created clusters and queued AI task
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AutoClusterResponse"
        "400":
          description: Invalid project or no keywords to cluster
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"

  /report-schedules:
    get:
      operationId: listReportSchedules
      tags: [report-schedules]
      responses:
        "200":
          description: Tenant report schedules
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/ReportSchedule"
    post:
      operationId: createReportSchedule
      tags: [report-schedules]
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/ReportScheduleBody"
      responses:
        "201":
          description: Created report schedule
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ReportSchedule"
        "400":
          description: Invalid report schedule payload
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"

  /projects/{projectId}/report-schedules:
    get:
      operationId: listProjectReportSchedules
      tags: [report-schedules]
      parameters:
        - name: projectId
          in: path
          required: true
          schema:
            type: integer
      responses:
        "200":
          description: Report schedules for a project
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/ReportSchedule"

  /report-schedules/{id}:
    patch:
      operationId: updateReportSchedule
      tags: [report-schedules]
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/UpdateReportScheduleBody"
      responses:
        "200":
          description: Updated report schedule
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ReportSchedule"
        "404":
          description: Schedule not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
    delete:
      operationId: deleteReportSchedule
      tags: [report-schedules]
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
      responses:
        "200":
          description: Deleted report schedule
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/OkResponse"

  /templates:
    get:
      operationId: listProjectTemplates
      tags: [templates]
      responses:
        "200":
          description: Project templates
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/ProjectTemplate"
    post:
      operationId: createProjectTemplate
      tags: [templates]
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/TemplateBody"
      responses:
        "201":
          description: Created project template
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ProjectTemplate"

  /templates/{id}:
    patch:
      operationId: updateProjectTemplate
      tags: [templates]
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/UpdateTemplateBody"
      responses:
        "200":
          description: Updated project template
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ProjectTemplate"
        "404":
          description: Template not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
    delete:
      operationId: deleteProjectTemplate
      tags: [templates]
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
      responses:
        "204":
          description: Deleted project template

  /projects/{projectId}/save-as-template:
    post:
      operationId: saveProjectAsTemplate
      tags: [templates]
      parameters:
        - name: projectId
          in: path
          required: true
          schema:
            type: integer
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/SaveProjectAsTemplateBody"
      responses:
        "201":
          description: Project saved as a reusable template
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ProjectTemplate"
        "404":
          description: Project not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"

  /usage:
    get:
      operationId: getUsageReport
      tags: [usage]
      responses:
        "200":
          description: Tenant quota usage report
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/UsageReport"

  /billing/webhook:
    post:
      operationId: handleBillingWebhook
      tags: [billing]
      parameters:
        - name: stripe-signature
          in: header
          required: true
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
      responses:
        "200":
          description: Stripe webhook accepted
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/StripeWebhookResponse"
        "400":
          description: Invalid Stripe webhook
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"

components:
  parameters:
    GeoAeoAuditId:
      name: auditId
      in: path
      required: true
      schema:
        type: integer

  schemas:
    HealthStatus:
      type: object
      properties:
        status:
          type: string
          enum: [ok]
      required:
        - status

    ErrorResponse:
      type: object
      properties:
        error:
          type: string
      required:
        - error

    RegisterBody:
      type: object
      properties:
        email:
          type: string
        password:
          type: string
        fullName:
          type: string
        tenantName:
          type: string
      required:
        - email
        - password
        - fullName
        - tenantName

    LoginBody:
      type: object
      properties:
        email:
          type: string
        password:
          type: string
      required:
        - email
        - password

    AuthResponse:
      type: object
      properties:
        user:
          $ref: "#/components/schemas/User"
        tenant:
          $ref: "#/components/schemas/Tenant"
      required:
        - user
        - tenant

    User:
      type: object
      properties:
        id:
          type: integer
        tenantId:
          type: integer
        email:
          type: string
        fullName:
          type: string
        role:
          type: string
          enum: [super_admin, agency_admin, agency_user, client]
        avatarUrl:
          type: ["string", "null"]
        lastLoginAt:
          type: ["string", "null"]
        createdAt:
          type: string
        updatedAt:
          type: string
      required:
        - id
        - tenantId
        - email
        - fullName
        - role
        - createdAt
        - updatedAt

    Tenant:
      type: object
      properties:
        id:
          type: integer
        name:
          type: string
        plan:
          type: string
          enum: [solo, agency, enterprise]
        seatsUsed:
          type: integer
        seatsMax:
          type: integer
        whiteLabelConfig:
          type: ["object", "null"]
        stripeCustomerId:
          type: ["string", "null"]
        stripeSubscriptionId:
          type: ["string", "null"]
        stripeSubscriptionStatus:
          type: ["string", "null"]
        stripeCurrentPeriodEnd:
          type: ["string", "null"]
        stripeCancelAtPeriodEnd:
          type: boolean
        createdAt:
          type: string
        updatedAt:
          type: string
      required:
        - id
        - name
        - plan
        - seatsUsed
        - seatsMax
        - stripeCancelAtPeriodEnd
        - createdAt
        - updatedAt

    UpdateTenantBody:
      type: object
      properties:
        name:
          type: string
        whiteLabelConfig:
          type: object

    DashboardSummary:
      type: object
      properties:
        clientCount:
          type: integer
        projectCount:
          type: integer
        keywordCount:
          type: integer
        clusterCount:
          type: integer
        briefCount:
          type: integer
        pendingApprovals:
          type: integer
        aiTasksThisMonth:
          type: integer
      required:
        - clientCount
        - projectCount
        - keywordCount
        - clusterCount
        - briefCount
        - pendingApprovals
        - aiTasksThisMonth

    Client:
      type: object
      properties:
        id:
          type: integer
        tenantId:
          type: integer
        name:
          type: string
        domain:
          type: ["string", "null"]
        industry:
          type: ["string", "null"]
        logoUrl:
          type: ["string", "null"]
        isActive:
          type: boolean
        createdAt:
          type: string
        updatedAt:
          type: string
      required:
        - id
        - tenantId
        - name
        - isActive
        - createdAt
        - updatedAt

    CreateClientBody:
      type: object
      properties:
        name:
          type: string
        domain:
          type: string
        industry:
          type: string
      required:
        - name

    UpdateClientBody:
      type: object
      properties:
        name:
          type: string
        domain:
          type: string
        industry:
          type: string
        logoUrl:
          type: string
        isActive:
          type: boolean

    Project:
      type: object
      properties:
        id:
          type: integer
        clientId:
          type: integer
        tenantId:
          type: integer
        name:
          type: string
        targetDomain:
          type: ["string", "null"]
        locale:
          type: string
        status:
          type: string
          enum: [active, paused, archived]
        createdAt:
          type: string
        updatedAt:
          type: string
      required:
        - id
        - clientId
        - tenantId
        - name
        - locale
        - status
        - createdAt
        - updatedAt

    CreateProjectBody:
      type: object
      properties:
        clientId:
          type: integer
        name:
          type: string
        targetDomain:
          type: string
        locale:
          type: string
      required:
        - clientId
        - name

    UpdateProjectBody:
      type: object
      properties:
        name:
          type: string
        targetDomain:
          type: string
        locale:
          type: string
        status:
          type: string
          enum: [active, paused, archived]

    Keyword:
      type: object
      properties:
        id:
          type: integer
        projectId:
          type: integer
        tenantId:
          type: integer
        phrase:
          type: string
        searchVolume:
          type: ["integer", "null"]
        cpc:
          type: ["number", "null"]
        kd:
          type: ["integer", "null"]
        intent:
          type: ["string", "null"]
        clusterId:
          type: ["integer", "null"]
        source:
          type: string
          enum: [manual, csv, ahrefs, semrush]
        isActive:
          type: boolean
        rawScore:
          type: ["number", "null"]
        finalScore:
          type: ["number", "null"]
        createdAt:
          type: string
        updatedAt:
          type: string
      required:
        - id
        - projectId
        - tenantId
        - phrase
        - source
        - isActive
        - createdAt
        - updatedAt

    CreateKeywordBody:
      type: object
      properties:
        phrase:
          type: string
        searchVolume:
          type: integer
        cpc:
          type: number
        kd:
          type: integer
        intent:
          type: string
          enum: [informational, navigational, commercial, transactional]
      required:
        - phrase

    UpdateKeywordBody:
      type: object
      properties:
        phrase:
          type: string
        searchVolume:
          type: integer
        cpc:
          type: number
        kd:
          type: integer
        intent:
          type: string
          enum: [informational, navigational, commercial, transactional]
        isActive:
          type: boolean
        clusterId:
          type: integer

    ImportKeywordsBody:
      type: object
      properties:
        source:
          type: string
          enum: [manual, csv, ahrefs, semrush]
        keywords:
          type: array
          items:
            $ref: "#/components/schemas/RawKeywordInput"
      required:
        - source
        - keywords

    RawKeywordInput:
      type: object
      properties:
        phrase:
          type: string
        searchVolume:
          type: integer
        cpc:
          type: number
        kd:
          type: integer
        intent:
          type: string
      required:
        - phrase

    ImportKeywordsResponse:
      type: object
      properties:
        imported:
          type: integer
        duplicates:
          type: integer
        total:
          type: integer
      required:
        - imported
        - duplicates
        - total

    ScoreSettings:
      type: object
      properties:
        projectId:
          type: integer
        volumeWeight:
          type: number
        kdWeight:
          type: number
        intentWeight:
          type: number
        cpcWeight:
          type: number
        freshnessWeight:
          type: number
      required:
        - projectId
        - volumeWeight
        - kdWeight
        - intentWeight
        - cpcWeight
        - freshnessWeight

    UpdateScoreSettingsBody:
      type: object
      properties:
        volumeWeight:
          type: number
        kdWeight:
          type: number
        intentWeight:
          type: number
        cpcWeight:
          type: number
        freshnessWeight:
          type: number

    KeywordCluster:
      type: object
      properties:
        id:
          type: integer
        projectId:
          type: integer
        tenantId:
          type: integer
        label:
          type: string
        pillarTopic:
          type: ["string", "null"]
        clusterType:
          type: string
          enum: [pillar, cluster, supporting]
        status:
          type: string
          enum: [pending, approved, rejected]
        keywordCount:
          type: integer
        avgScore:
          type: ["number", "null"]
        createdAt:
          type: string
        updatedAt:
          type: string
      required:
        - id
        - projectId
        - tenantId
        - label
        - clusterType
        - status
        - keywordCount
        - createdAt
        - updatedAt

    CreateClusterBody:
      type: object
      properties:
        label:
          type: string
        pillarTopic:
          type: string
        clusterType:
          type: string
          enum: [pillar, cluster, supporting]
      required:
        - label

    UpdateClusterBody:
      type: object
      properties:
        label:
          type: string
        pillarTopic:
          type: string
        clusterType:
          type: string
          enum: [pillar, cluster, supporting]

    AiTask:
      type: object
      properties:
        id:
          type: integer
        projectId:
          type: ["integer", "null"]
        tenantId:
          type: integer
        taskType:
          type: string
          enum: [cluster_keywords, generate_brief]
        provider:
          type: string
          enum: [mock, openai, anthropic]
        status:
          type: string
          enum: [queued, running, awaiting_approval, approved, rejected, completed, failed]
        input:
          type: object
        output:
          type: ["object", "null"]
        error:
          type: ["string", "null"]
        createdBy:
          type: integer
        approvedBy:
          type: ["integer", "null"]
        createdAt:
          type: string
        updatedAt:
          type: string
      required:
        - id
        - tenantId
        - taskType
        - provider
        - status
        - input
        - createdBy
        - createdAt
        - updatedAt

    TopicMap:
      type: object
      properties:
        projectId:
          type: integer
        authorityScore:
          type: number
        totalClusters:
          type: integer
        approvedClusters:
          type: integer
        pillars:
          type: array
          items:
            $ref: "#/components/schemas/TopicPillar"
      required:
        - projectId
        - authorityScore
        - totalClusters
        - approvedClusters
        - pillars

    TopicPillar:
      type: object
      properties:
        id:
          type: integer
        label:
          type: string
        keywordCount:
          type: integer
        clusters:
          type: array
          items:
            $ref: "#/components/schemas/KeywordCluster"
      required:
        - id
        - label
        - keywordCount
        - clusters

    RoadmapItem:
      type: object
      properties:
        clusterId:
          type: integer
        clusterLabel:
          type: string
        pillarTopic:
          type: ["string", "null"]
        keywordCount:
          type: integer
        avgScore:
          type: ["number", "null"]
        clusterStatus:
          type: string
        briefStatus:
          type: ["string", "null"]
        briefId:
          type: ["integer", "null"]
        priority:
          type: integer
      required:
        - clusterId
        - clusterLabel
        - keywordCount
        - clusterStatus
        - priority

    ContentBrief:
      type: object
      properties:
        id:
          type: integer
        clusterId:
          type: ["integer", "null"]
        projectId:
          type: integer
        tenantId:
          type: integer
        title:
          type: string
        outline:
          type: ["object", "null"]
        targetWordCount:
          type: ["integer", "null"]
        status:
          type: string
          enum: [draft, approved, in_progress, published]
        assignedTo:
          type: ["integer", "null"]
        createdAt:
          type: string
        updatedAt:
          type: string
      required:
        - id
        - projectId
        - tenantId
        - title
        - status
        - createdAt
        - updatedAt

    CreateBriefBody:
      type: object
      properties:
        clusterId:
          type: integer
        title:
          type: string
        targetWordCount:
          type: integer
      required:
        - title

    UpdateBriefBody:
      type: object
      properties:
        title:
          type: string
        outline:
          type: object
        targetWordCount:
          type: integer
        status:
          type: string
          enum: [draft, approved, in_progress, published]
        assignedTo:
          type: integer

    Report:
      type: object
      properties:
        id:
          type: integer
        projectId:
          type: integer
        tenantId:
          type: integer
        type:
          type: string
          enum: [project_summary, topical_authority, content_pipeline, geo_aeo_visibility_audit]
        format:
          type: string
          enum: [pdf, csv, json, markdown]
        generatedAt:
          type: ["string", "null"]
        fileUrl:
          type: ["string", "null"]
        data:
          type: ["object", "null"]
        createdAt:
          type: string
      required:
        - id
        - projectId
        - tenantId
        - type
        - format
        - createdAt

    GenerateReportBody:
      type: object
      properties:
        type:
          type: string
          enum: [project_summary, topical_authority, content_pipeline]
        format:
          type: string
          enum: [pdf, csv, json]
      required:
        - type
        - format

    GeoAeoEngine:
      type: string
      enum: [chatgpt, gemini, perplexity, google_ai_overviews, other]

    GeoAeoCaptureMethod:
      type: string
      enum: [manual_paste, csv_import, mock_adapter, api_adapter]

    GeoAeoAudit:
      type: object
      properties:
        id:
          type: integer
        tenantId:
          type: integer
        clientId:
          type: integer
        projectId:
          type: ["integer", "null"]
        auditName:
          type: string
        websiteUrl:
          type: string
        niche:
          type: string
        servicesOrProducts:
          type: ["array", "null"]
          items:
            type: string
        targetLocation:
          type: ["string", "null"]
        targetAudience:
          type: ["string", "null"]
        status:
          type: string
          enum: [draft, in_review, approved, archived]
        monitoringEnabled:
          type: boolean
        monitoringCadence:
          type: string
          enum: [none, monthly, quarterly]
        nextMonitoringRunAt:
          type: ["string", "null"]
        businessFacts:
          type: ["object", "null"]
          additionalProperties: true
        visibilityScore:
          type: ["number", "null"]
        visibilityLabel:
          type: ["string", "null"]
        summary:
          type: ["string", "null"]
        createdById:
          type: ["integer", "null"]
        updatedById:
          type: ["integer", "null"]
        approvedById:
          type: ["integer", "null"]
        approvedAt:
          type: ["string", "null"]
        createdAt:
          type: string
        updatedAt:
          type: string
      required:
        - id
        - tenantId
        - clientId
        - auditName
        - websiteUrl
        - niche
        - status
        - monitoringEnabled
        - monitoringCadence
        - createdAt
        - updatedAt

    GeoAeoAuditCreateBody:
      type: object
      properties:
        clientId:
          type: integer
        projectId:
          type: integer
        auditName:
          type: string
        websiteUrl:
          type: string
        niche:
          type: string
        servicesOrProducts:
          type: array
          items:
            type: string
        targetLocation:
          type: string
        targetAudience:
          type: string
        businessFacts:
          type: object
          additionalProperties: true
        targetEngines:
          type: array
          items:
            $ref: "#/components/schemas/GeoAeoEngine"
        monitoringEnabled:
          type: boolean
        monitoringCadence:
          type: string
          enum: [none, monthly, quarterly]
        nextMonitoringRunAt:
          type: string
      required:
        - clientId
        - auditName
        - websiteUrl
        - niche
        - servicesOrProducts
        - targetEngines

    GeoAeoAuditUpdateBody:
      type: object
      properties:
        auditName:
          type: string
        websiteUrl:
          type: string
        niche:
          type: string
        servicesOrProducts:
          type: array
          items:
            type: string
        targetLocation:
          type: ["string", "null"]
        targetAudience:
          type: ["string", "null"]
        businessFacts:
          type: object
          additionalProperties: true
        status:
          type: string
          enum: [draft, in_review, approved, archived]
        summary:
          type: ["string", "null"]
        monitoringEnabled:
          type: boolean
        monitoringCadence:
          type: string
          enum: [none, monthly, quarterly]
        nextMonitoringRunAt:
          type: ["string", "null"]

    GeoAeoPrompt:
      type: object
      properties:
        id:
          type: integer
        tenantId:
          type: integer
        auditId:
          type: integer
        promptSetId:
          type: ["integer", "null"]
        promptText:
          type: string
        normalizedPrompt:
          type: ["string", "null"]
        intent:
          type: ["string", "null"]
        funnelStage:
          type: ["string", "null"]
        serviceOrProduct:
          type: ["string", "null"]
        location:
          type: ["string", "null"]
        priority:
          type: integer
        status:
          type: string
        createdAt:
          type: string
        updatedAt:
          type: string
      required:
        - id
        - tenantId
        - auditId
        - promptText
        - priority
        - status
        - createdAt
        - updatedAt

    GeoAeoPromptCreateBody:
      type: object
      properties:
        auditId:
          type: integer
        promptSetId:
          type: integer
        promptText:
          type: string
        intent:
          type: string
        funnelStage:
          type: string
        serviceOrProduct:
          type: string
        location:
          type: string
        priority:
          type: integer
      required:
        - auditId
        - promptText

    GeoAeoCsvImportBody:
      type: object
      properties:
        csvText:
          type: string
      required:
        - csvText

    GeoAeoCsvPreviewIssue:
      type: object
      properties:
        row:
          type: integer
        reason:
          type: string
      required:
        - row
        - reason

    GeoAeoCsvImportPreview:
      type: object
      properties:
        totalRows:
          type: integer
        validRows:
          type: integer
        invalidRows:
          type: integer
        duplicateRows:
          type: integer
        invalid:
          type: array
          items:
            $ref: "#/components/schemas/GeoAeoCsvPreviewIssue"
        duplicates:
          type: array
          items:
            $ref: "#/components/schemas/GeoAeoCsvPreviewIssue"
      required:
        - totalRows
        - validRows
        - invalidRows
        - duplicateRows
        - invalid
        - duplicates

    GeoAeoPromptImportResult:
      type: object
      properties:
        imported:
          type: integer
        prompts:
          type: array
          items:
            $ref: "#/components/schemas/GeoAeoPrompt"
      required:
        - imported
        - prompts

    GeoAeoAnswerSnapshot:
      type: object
      properties:
        id:
          type: integer
        tenantId:
          type: integer
        auditId:
          type: integer
        promptId:
          type: integer
        promptVariantId:
          type: ["integer", "null"]
        importBatchId:
          type: ["integer", "null"]
        engine:
          $ref: "#/components/schemas/GeoAeoEngine"
        engineMode:
          type: string
        captureMethod:
          $ref: "#/components/schemas/GeoAeoCaptureMethod"
        answerText:
          type: string
        answerHash:
          type: string
        locationContext:
          type: ["string", "null"]
        clientMentioned:
          type: boolean
        clientCited:
          type: boolean
        sentiment:
          type: ["string", "null"]
        accuracyRiskScore:
          type: ["number", "null"]
        capturedAt:
          type: string
        createdAt:
          type: string
        updatedAt:
          type: string
      required:
        - id
        - tenantId
        - auditId
        - promptId
        - engine
        - engineMode
        - captureMethod
        - answerText
        - answerHash
        - clientMentioned
        - clientCited
        - capturedAt
        - createdAt
        - updatedAt

    GeoAeoSnapshotCreateBody:
      type: object
      properties:
        auditId:
          type: integer
        promptId:
          type: integer
        promptVariantId:
          type: integer
        engine:
          $ref: "#/components/schemas/GeoAeoEngine"
        captureMethod:
          $ref: "#/components/schemas/GeoAeoCaptureMethod"
        answerText:
          type: string
        capturedAt:
          type: string
        locationContext:
          type: string
      required:
        - auditId
        - promptId
        - engine
        - captureMethod
        - answerText

    GeoAeoSnapshotImportResult:
      type: object
      properties:
        imported:
          type: integer
        batch:
          $ref: "#/components/schemas/GeoAeoSnapshotImportBatch"
        snapshots:
          type: array
          items:
            $ref: "#/components/schemas/GeoAeoAnswerSnapshot"
      required:
        - imported
        - batch
        - snapshots

    GeoAeoSnapshotImportBatch:
      type: object
      properties:
        id:
          type: integer
        tenantId:
          type: integer
        auditId:
          type: integer
        importType:
          type: string
        source:
          type: string
        status:
          type: string
        totalRows:
          type: integer
        importedRows:
          type: integer
        invalidRows:
          type: integer
        duplicateRows:
          type: integer
        createdById:
          type: ["integer", "null"]
        deletedById:
          type: ["integer", "null"]
        createdAt:
          type: string
        updatedAt:
          type: string
        deletedAt:
          type: ["string", "null"]
      required:
        - id
        - tenantId
        - auditId
        - importType
        - source
        - status
        - totalRows
        - importedRows
        - invalidRows
        - duplicateRows
        - createdAt
        - updatedAt

    GeoAeoSnapshotImportRollbackResult:
      type: object
      properties:
        batch:
          $ref: "#/components/schemas/GeoAeoSnapshotImportBatch"
        rolledBackSnapshots:
          type: integer
      required:
        - batch
        - rolledBackSnapshots

    GeoAeoManualRecord:
      type: object
      additionalProperties: true
      properties:
        id:
          type: integer
        tenantId:
          type: integer
        auditId:
          type: integer
        status:
          type: string
        createdAt:
          type: string
        updatedAt:
          type: string

    GeoAeoSnapshotUpdateBody:
      type: object
      properties:
        clientMentioned:
          type: boolean
        clientCited:
          type: boolean
        sentiment:
          type: ["string", "null"]
        accuracyRiskScore:
          type: ["number", "null"]
        locationContext:
          type: ["string", "null"]

    GeoAeoCompetitorCreateBody:
      type: object
      properties:
        name:
          type: string
        websiteUrl:
          type: string
        aliases:
          type: array
          items:
            type: string
        notes:
          type: string
      required:
        - name

    GeoAeoCompetitorUpdateBody:
      type: object
      properties:
        name:
          type: string
        websiteUrl:
          type: string
        aliases:
          type: array
          items:
            type: string
        notes:
          type: string

    GeoAeoCitationCreateBody:
      type: object
      properties:
        snapshotId:
          type: integer
        url:
          type: string
        sourceName:
          type: string
        sourceType:
          type: string
        isClientOwned:
          type: boolean
        isCompetitorOwned:
          type: boolean
        authorityEstimate:
          type: number
        notes:
          type: string

    GeoAeoSourceRecommendationCreateBody:
      type: object
      properties:
        sourceName:
          type: string
        sourceUrl:
          type: string
        sourceType:
          type: string
        reason:
          type: string
        priority:
          type: string
          enum: [low, medium, high, critical]
        status:
          type: string
          enum: [draft, approved, rejected, done]
      required:
        - sourceName

    GeoAeoSourceRecommendationUpdateBody:
      type: object
      properties:
        sourceName:
          type: string
        sourceUrl:
          type: string
        sourceType:
          type: string
        reason:
          type: string
        priority:
          type: string
          enum: [low, medium, high, critical]
        status:
          type: string
          enum: [draft, approved, rejected, done]

    GeoAeoSchemaFindingCreateBody:
      type: object
      properties:
        pageUrl:
          type: string
        schemaType:
          type: string
        issueType:
          type: string
        severity:
          type: string
          enum: [low, medium, high, critical]
        recommendation:
          type: string
        status:
          type: string
          enum: [draft, approved, rejected, done]
      required:
        - issueType

    GeoAeoSchemaFindingUpdateBody:
      type: object
      properties:
        pageUrl:
          type: string
        schemaType:
          type: string
        issueType:
          type: string
        severity:
          type: string
          enum: [low, medium, high, critical]
        recommendation:
          type: string
        status:
          type: string
          enum: [draft, approved, rejected, done]

    GeoAeoMonitoringRun:
      type: object
      properties:
        id:
          type: integer
        tenantId:
          type: integer
        auditId:
          type: integer
        runMonth:
          type: string
          pattern: "^\\d{4}-(0[1-9]|1[0-2])$"
        baselineMonth:
          type: ["string", "null"]
          pattern: "^\\d{4}-(0[1-9]|1[0-2])$"
        comparisonMonth:
          type: string
          pattern: "^\\d{4}-(0[1-9]|1[0-2])$"
        status:
          type: string
          enum: [draft, in_review, approved, archived]
        baselineScore:
          type: ["number", "null"]
        currentScore:
          type: ["number", "null"]
        scoreDelta:
          type: ["number", "null"]
        baselineSnapshotCount:
          type: integer
        currentSnapshotCount:
          type: integer
        actionPlanTemplate:
          type: ["object", "null"]
          additionalProperties: true
        reportTemplate:
          type: ["object", "null"]
          additionalProperties: true
        summary:
          type: ["string", "null"]
        notes:
          type: ["string", "null"]
        approvedAt:
          type: ["string", "null"]
        createdAt:
          type: string
        updatedAt:
          type: string
      required:
        - id
        - tenantId
        - auditId
        - runMonth
        - comparisonMonth
        - status
        - baselineSnapshotCount
        - currentSnapshotCount
        - createdAt
        - updatedAt

    GeoAeoMonitoringRunCreateBody:
      type: object
      properties:
        runMonth:
          type: string
          pattern: "^\\d{4}-(0[1-9]|1[0-2])$"
        baselineMonth:
          type: string
          pattern: "^\\d{4}-(0[1-9]|1[0-2])$"
        comparisonMonth:
          type: string
          pattern: "^\\d{4}-(0[1-9]|1[0-2])$"
        baselineScore:
          type: number
        currentScore:
          type: number
        baselineSnapshotCount:
          type: integer
        currentSnapshotCount:
          type: integer
        actionPlanTemplate:
          type: object
          additionalProperties: true
        reportTemplate:
          type: object
          additionalProperties: true
        summary:
          type: string
        notes:
          type: string
      required:
        - runMonth

    GeoAeoMonitoringRunUpdateBody:
      type: object
      properties:
        runMonth:
          type: string
          pattern: "^\\d{4}-(0[1-9]|1[0-2])$"
        baselineMonth:
          type: string
          pattern: "^\\d{4}-(0[1-9]|1[0-2])$"
        comparisonMonth:
          type: string
          pattern: "^\\d{4}-(0[1-9]|1[0-2])$"
        baselineScore:
          type: number
        currentScore:
          type: number
        baselineSnapshotCount:
          type: integer
        currentSnapshotCount:
          type: integer
        actionPlanTemplate:
          type: object
          additionalProperties: true
        reportTemplate:
          type: object
          additionalProperties: true
        summary:
          type: string
        notes:
          type: string
        status:
          type: string
          enum: [draft, in_review, approved, archived]

    GeoAeoFinding:
      type: object
      properties:
        id:
          type: integer
        tenantId:
          type: integer
        auditId:
          type: integer
        findingType:
          type: string
        severity:
          type: string
        title:
          type: string
        description:
          type: ["string", "null"]
        evidence:
          type: ["object", "null"]
          additionalProperties: true
        recommendation:
          type: ["string", "null"]
        status:
          type: string
          enum: [draft, needs_review, approved, rejected, deleted, superseded]
        approvedAt:
          type: ["string", "null"]
        createdAt:
          type: string
        updatedAt:
          type: string
      required:
        - id
        - tenantId
        - auditId
        - findingType
        - severity
        - title
        - status
        - createdAt
        - updatedAt

    GeoAeoFindingUpdateBody:
      type: object
      properties:
        findingType:
          type: string
        title:
          type: string
        description:
          type: string
        recommendation:
          type: string
        status:
          type: string
          enum: [draft, needs_review, approved, rejected, deleted]

    GeoAeoScoreBody:
      type: object
      properties:
        brandMentionCoverage:
          type: number
        citationCoverage:
          type: number
        promptIntentCoverage:
          type: number
        competitorGapOpportunity:
          type: number
        entityClarityScore:
          type: number
        schemaReadinessScore:
          type: number
        sourceAuthorityReadiness:
          type: number
        accuracyRiskScore:
          type: number
        score:
          type: number
        reason:
          type: string

    GeoAeoVisibilityScoreResult:
      type: object
      properties:
        score:
          type: number
        label:
          type: string
        normalizedInputs:
          type: object
          additionalProperties: true
        explanations:
          type: array
          items:
            type: string
      required:
        - score
        - label
        - normalizedInputs
        - explanations

    GeoAeoStoredScoreResult:
      type: object
      properties:
        score:
          type: object
          additionalProperties: true
        result:
          $ref: "#/components/schemas/GeoAeoVisibilityScoreResult"
      required:
        - score
        - result

    GeoAeoAnalysisResult:
      type: object
      properties:
        score:
          $ref: "#/components/schemas/GeoAeoVisibilityScoreResult"
        inserted:
          type: object
          properties:
            mentions:
              type: integer
            citations:
              type: integer
            findings:
              type: integer
          required:
            - mentions
            - citations
            - findings
      required:
        - score
        - inserted

    GeoAeoActionPlan:
      type: object
      properties:
        id:
          type: integer
        tenantId:
          type: integer
        auditId:
          type: integer
        name:
          type: string
        timeHorizonDays:
          type: integer
        summary:
          type: ["string", "null"]
        status:
          type: string
        createdAt:
          type: string
        updatedAt:
          type: string
      required:
        - id
        - tenantId
        - auditId
        - name
        - timeHorizonDays
        - status
        - createdAt
        - updatedAt

    GeoAeoActionItem:
      type: object
      properties:
        id:
          type: integer
        tenantId:
          type: integer
        auditId:
          type: integer
        actionPlanId:
          type: integer
        title:
          type: string
        description:
          type: ["string", "null"]
        category:
          type: string
        priority:
          type: string
          enum: [low, medium, high, critical]
        weekNumber:
          type: integer
        ownerRole:
          type: ["string", "null"]
        status:
          type: string
          enum: [draft, approved, in_progress, done, deferred]
        relatedFindingId:
          type: ["integer", "null"]
        createdAt:
          type: string
        updatedAt:
          type: string
      required:
        - id
        - tenantId
        - auditId
        - actionPlanId
        - title
        - category
        - priority
        - weekNumber
        - status
        - createdAt
        - updatedAt

    GeoAeoActionPlanWithItems:
      type: object
      properties:
        plan:
          $ref: "#/components/schemas/GeoAeoActionPlan"
        items:
          type: array
          items:
            $ref: "#/components/schemas/GeoAeoActionItem"
      required:
        - plan
        - items

    GeoAeoActionPlanGenerateBody:
      type: object
      properties:
        name:
          type: string
        timeHorizonDays:
          type: integer

    GeoAeoActionItemCreateBody:
      type: object
      properties:
        title:
          type: string
        description:
          type: string
        category:
          type: string
        priority:
          type: string
          enum: [low, medium, high, critical]
        weekNumber:
          type: integer
        status:
          type: string
          enum: [draft, approved, in_progress, done, deferred]
      required:
        - title

    GeoAeoActionItemUpdateBody:
      type: object
      properties:
        title:
          type: string
        description:
          type: string
        category:
          type: string
        priority:
          type: string
          enum: [low, medium, high, critical]
        weekNumber:
          type: integer
        status:
          type: string
          enum: [draft, approved, in_progress, done, deferred]

    GeoAeoActionPlanItemResult:
      type: object
      properties:
        plan:
          $ref: "#/components/schemas/GeoAeoActionPlan"
        item:
          $ref: "#/components/schemas/GeoAeoActionItem"
      required:
        - plan
        - item

    GeoAeoReportGenerateBody:
      type: object
      properties:
        format:
          type: string
          enum: [markdown, csv, json, pdf]

    GeoAeoReportExportBody:
      type: object
      properties:
        format:
          type: string
          enum: [markdown, csv, json, pdf]

    GeoAeoReport:
      allOf:
        - $ref: "#/components/schemas/Report"

    GeoAeoClientAuditDetail:
      type: object
      properties:
        audit:
          $ref: "#/components/schemas/GeoAeoAudit"
        findings:
          type: array
          items:
            $ref: "#/components/schemas/GeoAeoFinding"
        actionPlan:
          oneOf:
            - $ref: "#/components/schemas/GeoAeoActionPlanWithItems"
            - type: "null"
        reports:
          type: array
          items:
            $ref: "#/components/schemas/GeoAeoReport"
        monitoringRuns:
          type: array
          items:
            $ref: "#/components/schemas/GeoAeoMonitoringRun"
        access:
          $ref: "#/components/schemas/GeoAeoClientDashboardAccess"
      required:
        - audit
        - findings
        - actionPlan
        - reports
        - monitoringRuns
        - access

    GeoAeoClientDashboardAccess:
      type: object
      properties:
        permission:
          type: string
          enum: [geoAeo.viewClientDashboard]
        clientView:
          type: boolean
        downloadsAllowed:
          type: boolean
        licensePlan:
          type: string
          nullable: true
      required:
        - permission
        - clientView
        - downloadsAllowed
        - licensePlan

    Plan:
      type: object
      properties:
        id:
          type: string
        name:
          type: string
          enum: [solo, agency, enterprise]
        displayName:
          type: string
        priceMonthly:
          type: integer
        seats:
          type: integer
        projectsMax:
          type: integer
        aiTasksPerMonth:
          type: integer
        whiteLabelEnabled:
          type: boolean
      required:
        - id
        - name
        - displayName
        - priceMonthly
        - seats
        - projectsMax
        - aiTasksPerMonth
        - whiteLabelEnabled

    Subscription:
      type: object
      properties:
        plan:
          type: string
          enum: [solo, agency, enterprise]
        status:
          type: string
        seatsUsed:
          type: integer
        seatsMax:
          type: integer
        currentPeriodEnd:
          type: ["string", "null"]
        stripeSubscriptionId:
          type: ["string", "null"]
        cancelAtPeriodEnd:
          type: boolean
      required:
        - plan
        - status
        - seatsUsed
        - seatsMax
        - cancelAtPeriodEnd

    CreateCheckoutBody:
      type: object
      properties:
        planId:
          type: string
      required:
        - planId

    CheckoutResponse:
      type: object
      properties:
        url:
          type: string
      required:
        - url

    UsageSummary:
      type: object
      properties:
        aiTasksThisMonth:
          type: integer
        aiTasksLimit:
          type: integer
        seatsUsed:
          type: integer
        seatsMax:
          type: integer
        projectsCount:
          type: integer
        projectsMax:
          type: integer
      required:
        - aiTasksThisMonth
        - aiTasksLimit
        - seatsUsed
        - seatsMax
        - projectsCount
        - projectsMax

    DetailedHealthStatus:
      type: object
      properties:
        status:
          type: string
          enum: [ok, degraded]
        timestamp:
          type: string
        uptimeSeconds:
          type: integer
        responseTimeMs:
          type: integer
        services:
          type: object
          properties:
            database:
              type: object
              properties:
                status:
                  type: string
                  enum: [ok, error]
                latencyMs:
                  type: integer
              required:
                - status
                - latencyMs
            ai:
              type: object
              properties:
                status:
                  type: string
                  enum: [configured, mock-fallback]
              required:
                - status
            smtp:
              type: object
              properties:
                status:
                  type: string
                  enum: [configured, mock-fallback]
              required:
                - status
            billing:
              type: object
              properties:
                status:
                  type: string
                  enum: [configured, missing-config, disabled]
                missing:
                  type: array
                  items:
                    type: string
              required:
                - status
                - missing
          required:
            - database
            - ai
            - smtp
            - billing
        memory:
          type: object
          properties:
            heapUsedMB:
              type: integer
            heapTotalMB:
              type: integer
            rssMB:
              type: integer
          required:
            - heapUsedMB
            - heapTotalMB
            - rssMB
        version:
          type: string
      required:
        - status
        - timestamp
        - uptimeSeconds
        - responseTimeMs
        - services
        - memory
        - version

    AnalyticsOverview:
      type: object
      properties:
        totals:
          type: object
          properties:
            clients:
              type: integer
            projects:
              type: integer
            keywords:
              type: integer
            clusters:
              type: integer
            briefs:
              type: integer
            reports:
              type: integer
            aiTasksThisMonth:
              type: integer
          required:
            - clients
            - projects
            - keywords
            - clusters
            - briefs
            - reports
            - aiTasksThisMonth
        keywordsBySource:
          type: array
          items:
            $ref: "#/components/schemas/AnalyticsSourceBucket"
        briefsByStatus:
          type: array
          items:
            $ref: "#/components/schemas/AnalyticsStatusBucket"
        clustersByStatus:
          type: array
          items:
            $ref: "#/components/schemas/AnalyticsStatusBucket"
      required:
        - totals
        - keywordsBySource
        - briefsByStatus
        - clustersByStatus

    AnalyticsSourceBucket:
      type: object
      properties:
        source:
          type: string
        c:
          type: integer
      required:
        - source
        - c

    AnalyticsStatusBucket:
      type: object
      properties:
        status:
          type: string
        c:
          type: integer
      required:
        - status
        - c

    AnalyticsProject:
      type: object
      properties:
        id:
          type: integer
        name:
          type: string
        createdAt:
          type: string
        keywordCount:
          type: integer
        clusterCount:
          type: integer
        briefCount:
          type: integer
      required:
        - id
        - name
        - createdAt
        - keywordCount
        - clusterCount
        - briefCount

    AnalyticsVelocityPoint:
      type: object
      properties:
        day:
          type: string
        count:
          type: integer
      required:
        - day
        - count

    AuditLogEntry:
      type: object
      properties:
        id:
          type: integer
        action:
          type: string
        resourceType:
          type: string
        resourceId:
          type: ["string", "null"]
        metadata:
          type: ["object", "null"]
        ipAddress:
          type: ["string", "null"]
        createdAt:
          type: string
        userName:
          type: ["string", "null"]
        userEmail:
          type: ["string", "null"]
      required:
        - id
        - action
        - resourceType
        - createdAt

    ContentCalendarStatus:
      type: string
      enum: [planned, in_progress, review, published]

    ContentCalendarEntry:
      type: object
      properties:
        id:
          type: integer
        tenantId:
          type: integer
        projectId:
          type: integer
        briefId:
          type: ["integer", "null"]
        title:
          type: string
        status:
          $ref: "#/components/schemas/ContentCalendarStatus"
        dueDate:
          type: ["string", "null"]
        publishedDate:
          type: ["string", "null"]
        assignedTo:
          type: ["integer", "null"]
        notes:
          type: ["string", "null"]
        createdAt:
          type: string
        updatedAt:
          type: string
      required:
        - id
        - tenantId
        - projectId
        - title
        - status
        - createdAt
        - updatedAt

    CalendarEntryBody:
      type: object
      properties:
        title:
          type: string
        status:
          $ref: "#/components/schemas/ContentCalendarStatus"
        dueDate:
          type: ["string", "null"]
        publishedDate:
          type: ["string", "null"]
        assignedTo:
          type: ["integer", "null"]
        briefId:
          type: ["integer", "null"]
        notes:
          type: ["string", "null"]
      required:
        - title

    UpdateCalendarEntryBody:
      type: object
      properties:
        title:
          type: string
        status:
          $ref: "#/components/schemas/ContentCalendarStatus"
        dueDate:
          type: ["string", "null"]
        publishedDate:
          type: ["string", "null"]
        assignedTo:
          type: ["integer", "null"]
        briefId:
          type: ["integer", "null"]
        notes:
          type: ["string", "null"]

    CommentEntityType:
      type: string
      enum: [cluster, brief, project, keyword]

    CommentUser:
      type: object
      properties:
        id:
          type: ["integer", "null"]
        fullName:
          type: string
        avatarUrl:
          type: ["string", "null"]
      required:
        - id
        - fullName

    Comment:
      type: object
      properties:
        id:
          type: integer
        tenantId:
          type: integer
        userId:
          type: integer
        body:
          type: string
        entityType:
          $ref: "#/components/schemas/CommentEntityType"
        entityId:
          type: integer
        resolvedAt:
          type: ["string", "null"]
        createdAt:
          type: string
        updatedAt:
          type: string
        user:
          $ref: "#/components/schemas/CommentUser"
      required:
        - id
        - body
        - entityType
        - entityId
        - createdAt
        - updatedAt

    CreateCommentBody:
      type: object
      properties:
        entityType:
          $ref: "#/components/schemas/CommentEntityType"
        entityId:
          type: integer
        body:
          type: string
      required:
        - entityType
        - entityId
        - body

    CustomFieldEntityType:
      type: string
      enum: [project, keyword, client]

    CustomFieldType:
      type: string
      enum: [text, number, select, date, boolean]

    CustomField:
      type: object
      properties:
        id:
          type: integer
        tenantId:
          type: integer
        entityType:
          $ref: "#/components/schemas/CustomFieldEntityType"
        name:
          type: string
        slug:
          type: string
        fieldType:
          $ref: "#/components/schemas/CustomFieldType"
        options:
          type: ["array", "null"]
          items:
            type: string
        isRequired:
          type: boolean
        createdAt:
          type: string
      required:
        - id
        - tenantId
        - entityType
        - name
        - slug
        - fieldType
        - isRequired
        - createdAt

    CustomFieldBody:
      type: object
      properties:
        entityType:
          $ref: "#/components/schemas/CustomFieldEntityType"
        name:
          type: string
        slug:
          type: string
        fieldType:
          $ref: "#/components/schemas/CustomFieldType"
        options:
          type: array
          items:
            type: string
        isRequired:
          type: boolean
      required:
        - entityType
        - name
        - slug
        - fieldType

    CustomFieldValue:
      type: object
      properties:
        id:
          type: integer
        tenantId:
          type: integer
        fieldId:
          type: integer
        entityType:
          $ref: "#/components/schemas/CustomFieldEntityType"
        entityId:
          type: integer
        value: {}
        createdAt:
          type: string
        updatedAt:
          type: string
      required:
        - id
        - tenantId
        - fieldId
        - entityType
        - entityId
        - createdAt
        - updatedAt

    UpsertCustomFieldValueBody:
      type: object
      properties:
        fieldId:
          type: integer
        entityType:
          $ref: "#/components/schemas/CustomFieldEntityType"
        entityId:
          type: integer
        value: {}
      required:
        - fieldId
        - entityType
        - entityId

    GdprExport:
      type: object
      properties:
        exportedAt:
          type: string
        user:
          type: ["object", "null"]
        comments:
          type: array
          items:
            type: object
        auditLogs:
          type: array
          items:
            type: object
        notifications:
          type: array
          items:
            $ref: "#/components/schemas/Notification"
        keywords:
          type: array
          items:
            $ref: "#/components/schemas/Keyword"
        contentBriefs:
          type: array
          items:
            $ref: "#/components/schemas/ContentBrief"
      required:
        - exportedAt
        - user
        - comments
        - auditLogs
        - notifications
        - keywords
        - contentBriefs

    GdprDeleteResponse:
      type: object
      properties:
        ok:
          type: boolean
        message:
          type: string
      required:
        - ok
        - message

    CompetitorDomain:
      type: object
      properties:
        id:
          type: integer
        tenantId:
          type: integer
        projectId:
          type: integer
        domain:
          type: string
        label:
          type: ["string", "null"]
        createdAt:
          type: string
      required:
        - id
        - tenantId
        - projectId
        - domain
        - createdAt

    CreateCompetitorBody:
      type: object
      properties:
        domain:
          type: string
        label:
          type: string
      required:
        - domain

    CompetitorCoverage:
      type: object
      properties:
        domain:
          type: string
        ranking:
          type: ["integer", "null"]
      required:
        - domain
        - ranking

    KeywordGapKeyword:
      allOf:
        - $ref: "#/components/schemas/Keyword"
        - type: object
          properties:
            competitorCoverage:
              type: array
              items:
                $ref: "#/components/schemas/CompetitorCoverage"
          required:
            - competitorCoverage

    KeywordGapResponse:
      type: object
      properties:
        keywords:
          type: array
          items:
            $ref: "#/components/schemas/KeywordGapKeyword"
        competitors:
          type: array
          items:
            $ref: "#/components/schemas/CompetitorDomain"
      required:
        - keywords
        - competitors

    ProjectExport:
      type: object
      properties:
        exportedAt:
          type: string
        version:
          type: string
        project:
          $ref: "#/components/schemas/Project"
        keywords:
          type: array
          items:
            $ref: "#/components/schemas/Keyword"
        clusters:
          type: array
          items:
            $ref: "#/components/schemas/KeywordCluster"
        briefs:
          type: array
          items:
            $ref: "#/components/schemas/ContentBrief"
        reports:
          type: array
          items:
            $ref: "#/components/schemas/Report"
        scoreSettings:
          type: ["object", "null"]
      required:
        - exportedAt
        - version
        - project
        - keywords
        - clusters
        - briefs
        - reports
        - scoreSettings

    KeywordRanking:
      type: object
      properties:
        id:
          type: integer
        phrase:
          type: string
        searchVolume:
          type: ["integer", "null"]
        latestPosition:
          type: ["integer", "null"]
        latestUrl:
          type: ["string", "null"]
        checkedAt:
          type: ["string", "null"]
      required:
        - id
        - phrase
        - latestPosition
        - latestUrl
        - checkedAt

    KeywordRankingHistoryEntry:
      type: object
      properties:
        id:
          type: integer
        tenantId:
          type: integer
        keywordId:
          type: integer
        position:
          type: ["integer", "null"]
        url:
          type: ["string", "null"]
        checkedAt:
          type: string
      required:
        - id
        - tenantId
        - keywordId
        - checkedAt

    CheckRankingBody:
      type: object
      properties:
        keywordId:
          type: integer
        position:
          type: ["integer", "null"]
        url:
          type: ["string", "null"]
      required:
        - keywordId
        - position

    BulkRankingCheckResponse:
      type: object
      properties:
        checked:
          type: integer
      required:
        - checked

    AutoClusterResponse:
      type: object
      properties:
        clusters:
          type: array
          items:
            $ref: "#/components/schemas/KeywordCluster"
        taskId:
          type: integer
      required:
        - clusters
        - taskId

    ReportScheduleType:
      type: string
      enum: [project_summary, topical_authority, content_pipeline]

    ReportScheduleFrequency:
      type: string
      enum: [daily, weekly, monthly]

    ReportSchedule:
      type: object
      properties:
        id:
          type: integer
        tenantId:
          type: integer
        projectId:
          type: integer
        reportType:
          $ref: "#/components/schemas/ReportScheduleType"
        frequency:
          $ref: "#/components/schemas/ReportScheduleFrequency"
        recipientEmails:
          type: array
          items:
            type: string
        lastSentAt:
          type: ["string", "null"]
        nextSendAt:
          type: ["string", "null"]
        isActive:
          type: boolean
        createdAt:
          type: string
        updatedAt:
          type: string
      required:
        - id
        - tenantId
        - projectId
        - reportType
        - frequency
        - recipientEmails
        - isActive
        - createdAt
        - updatedAt

    ReportScheduleBody:
      type: object
      properties:
        projectId:
          type: integer
        reportType:
          $ref: "#/components/schemas/ReportScheduleType"
        frequency:
          $ref: "#/components/schemas/ReportScheduleFrequency"
        recipientEmails:
          type: array
          items:
            type: string
        isActive:
          type: boolean
      required:
        - projectId
        - reportType
        - frequency
        - recipientEmails

    UpdateReportScheduleBody:
      type: object
      properties:
        projectId:
          type: integer
        reportType:
          $ref: "#/components/schemas/ReportScheduleType"
        frequency:
          $ref: "#/components/schemas/ReportScheduleFrequency"
        recipientEmails:
          type: array
          items:
            type: string
        isActive:
          type: boolean

    ProjectTemplate:
      type: object
      properties:
        id:
          type: integer
        tenantId:
          type: integer
        name:
          type: string
        description:
          type: ["string", "null"]
        config:
          type: object
        createdBy:
          type: ["integer", "null"]
        createdAt:
          type: string
        updatedAt:
          type: string
      required:
        - id
        - tenantId
        - name
        - config
        - createdAt
        - updatedAt

    TemplateBody:
      type: object
      properties:
        name:
          type: string
        description:
          type: string
        config:
          type: object
      required:
        - name

    UpdateTemplateBody:
      type: object
      properties:
        name:
          type: string
        description:
          type: string
        config:
          type: object

    SaveProjectAsTemplateBody:
      type: object
      properties:
        name:
          type: string
        description:
          type: string
      required:
        - name

    UsageReport:
      type: object
      properties:
        plan:
          type: string
        period:
          type: object
          properties:
            start:
              type: string
            end:
              type: string
          required:
            - start
            - end
        usage:
          type: object
          properties:
            keywords:
              $ref: "#/components/schemas/UsageQuota"
            briefs:
              $ref: "#/components/schemas/UsageQuota"
            aiTasks:
              $ref: "#/components/schemas/UsageQuota"
            seats:
              $ref: "#/components/schemas/UsageQuota"
            reportsThisMonth:
              type: integer
            apiKeys:
              type: integer
            webhookDeliveriesThisMonth:
              type: integer
          required:
            - keywords
            - briefs
            - aiTasks
            - seats
            - reportsThisMonth
            - apiKeys
            - webhookDeliveriesThisMonth
      required:
        - plan
        - period
        - usage

    UsageQuota:
      type: object
      properties:
        used:
          type: integer
        limit:
          type: integer
      required:
        - used
        - limit

    StripeWebhookResponse:
      type: object
      properties:
        received:
          type: boolean
      required:
        - received

    OkResponse:
      type: object
      properties:
        ok:
          type: boolean
      required:
        - ok

    MessageResponse:
      type: object
      properties:
        message:
          type: string
      required:
        - message

    TeamMember:
      type: object
      properties:
        id:
          type: integer
        email:
          type: string
        fullName:
          type: string
        role:
          type: string
          enum: [super_admin, agency_admin, agency_user, client]
        avatarUrl:
          type: ["string", "null"]
        lastLoginAt:
          type: ["string", "null"]
        createdAt:
          type: string
      required:
        - id
        - email
        - fullName
        - role
        - createdAt

    UpdateTeamMemberRoleBody:
      type: object
      properties:
        role:
          type: string
          enum: [agency_admin, agency_user, client]
      required:
        - role

    InviteTeamMemberBody:
      type: object
      properties:
        email:
          type: string
        role:
          type: string
          enum: [agency_admin, agency_user, client]
      required:
        - email

    TeamInviteResponse:
      type: object
      properties:
        id:
          type: integer
        email:
          type: string
        role:
          type: string
        token:
          type: string
        expiresAt:
          type: string
        inviteUrl:
          type: string
      required:
        - id
        - email
        - role
        - token
        - expiresAt
        - inviteUrl

    TeamInvitation:
      type: object
      properties:
        id:
          type: integer
        tenantId:
          type: integer
        email:
          type: string
        role:
          type: string
        token:
          type: string
        invitedBy:
          type: integer
        expiresAt:
          type: string
        acceptedAt:
          type: ["string", "null"]
        createdAt:
          type: string
      required:
        - id
        - tenantId
        - email
        - role
        - token
        - invitedBy
        - expiresAt
        - createdAt

    AcceptTeamInvitationBody:
      type: object
      properties:
        token:
          type: string
        fullName:
          type: string
        password:
          type: string
      required:
        - token
        - fullName
        - password

    AcceptTeamInvitationResponse:
      type: object
      properties:
        user:
          type: object
          properties:
            id:
              type: integer
            email:
              type: string
            fullName:
              type: string
            role:
              type: string
          required:
            - id
            - email
            - fullName
            - role
      required:
        - user

    IntegrationProvider:
      type: string
      enum: [ahrefs, semrush, dataforseo, google_search_console]

    KeywordSearchProvider:
      type: string
      enum: [ahrefs, semrush, dataforseo]

    Integration:
      type: object
      properties:
        id:
          type: integer
        provider:
          $ref: "#/components/schemas/IntegrationProvider"
        isActive:
          type: string
        createdAt:
          type: string
        updatedAt:
          type: string
      required:
        - id
        - provider
        - isActive

    ConfigureIntegrationBody:
      type: object
      properties:
        provider:
          $ref: "#/components/schemas/IntegrationProvider"
        credentials:
          type: object
          additionalProperties:
            type: string
      required:
        - provider
        - credentials

    SearchIntegrationKeywordsBody:
      type: object
      properties:
        query:
          type: string
      required:
        - query

    ProviderKeyword:
      type: object
      properties:
        phrase:
          type: string
        searchVolume:
          type: integer
        cpc:
          type: number
        kd:
          type: integer
        intent:
          type: string
        source:
          type: string
      required:
        - phrase

    ApiKeyScope:
      type: string
      enum: [read, write]

    ApiKey:
      type: object
      properties:
        id:
          type: integer
        name:
          type: string
        keyPrefix:
          type: string
        scopes:
          type: array
          items:
            $ref: "#/components/schemas/ApiKeyScope"
        lastUsedAt:
          type: ["string", "null"]
        expiresAt:
          type: ["string", "null"]
        revokedAt:
          type: ["string", "null"]
        createdAt:
          type: string
      required:
        - id
        - name
        - keyPrefix
        - scopes
        - createdAt

    CreateApiKeyBody:
      type: object
      properties:
        name:
          type: string
        scopes:
          type: array
          items:
            $ref: "#/components/schemas/ApiKeyScope"
        expiresInDays:
          type: integer
      required:
        - name

    CreateApiKeyResponse:
      type: object
      properties:
        id:
          type: integer
        name:
          type: string
        keyPrefix:
          type: string
        scopes:
          type: array
          items:
            $ref: "#/components/schemas/ApiKeyScope"
        expiresAt:
          type: ["string", "null"]
        createdAt:
          type: string
        key:
          type: string
      required:
        - id
        - name
        - keyPrefix
        - scopes
        - createdAt
        - key

    WebhookEvent:
      type: string
      enum:
        - keyword.imported
        - cluster.created
        - cluster.approved
        - cluster.rejected
        - brief.created
        - brief.approved
        - report.generated
        - project.created
        - ai_task.completed

    WebhookEndpoint:
      type: object
      properties:
        id:
          type: integer
        tenantId:
          type: integer
        url:
          type: string
        events:
          type: array
          items:
            $ref: "#/components/schemas/WebhookEvent"
        secret:
          type: "null"
        isActive:
          type: boolean
        description:
          type: ["string", "null"]
        createdAt:
          type: string
        updatedAt:
          type: string
      required:
        - id
        - tenantId
        - url
        - events
        - isActive
        - createdAt
        - updatedAt

    WebhookEndpointWithSecret:
      type: object
      properties:
        id:
          type: integer
        tenantId:
          type: integer
        url:
          type: string
        events:
          type: array
          items:
            $ref: "#/components/schemas/WebhookEvent"
        secret:
          type: string
        isActive:
          type: boolean
        description:
          type: ["string", "null"]
        createdAt:
          type: string
        updatedAt:
          type: string
      required:
        - id
        - tenantId
        - url
        - events
        - secret
        - isActive
        - createdAt
        - updatedAt

    CreateWebhookBody:
      type: object
      properties:
        url:
          type: string
        events:
          type: array
          items:
            $ref: "#/components/schemas/WebhookEvent"
        description:
          type: string
      required:
        - url

    UpdateWebhookBody:
      type: object
      properties:
        url:
          type: string
        events:
          type: array
          items:
            $ref: "#/components/schemas/WebhookEvent"
        isActive:
          type: boolean
        description:
          type: string

    WebhookDelivery:
      type: object
      properties:
        id:
          type: integer
        endpointId:
          type: integer
        tenantId:
          type: integer
        event:
          $ref: "#/components/schemas/WebhookEvent"
        payload:
          type: object
        status:
          type: string
        statusCode:
          type: ["integer", "null"]
        responseBody:
          type: ["string", "null"]
        attempts:
          type: integer
        lastAttemptAt:
          type: ["string", "null"]
        createdAt:
          type: string
      required:
        - id
        - endpointId
        - tenantId
        - event
        - payload
        - status
        - attempts
        - createdAt

    Notification:
      type: object
      properties:
        id:
          type: integer
        tenantId:
          type: integer
        userId:
          type: integer
        type:
          type: string
        title:
          type: string
        body:
          type: ["string", "null"]
        link:
          type: ["string", "null"]
        readAt:
          type: ["string", "null"]
        createdAt:
          type: string
      required:
        - id
        - tenantId
        - userId
        - type
        - title
        - createdAt

    UnreadNotificationCount:
      type: object
      properties:
        count:
          type: integer
      required:
        - count
